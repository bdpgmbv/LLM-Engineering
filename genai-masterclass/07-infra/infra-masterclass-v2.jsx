import { useState } from "react";
const D=[
{id:"engines",icon:"1",title:"Inference Engines",sub:"vLLM, TGI, TensorRT-LLM, Ollama, llama.cpp",intro:"Imagine you have a brilliant chef (your LLM model). The chef is the same regardless of the kitchen they work in. But the KITCHEN matters: a professional kitchen with industrial ovens serves 100 meals/hour. A home kitchen serves 5. Inference engines are the kitchen -- they determine how fast and efficiently your model serves predictions. vLLM is the professional kitchen.",topics:[
{name:"5 Engines Compared",teach:"5 KITCHENS FOR YOUR LLM CHEF:\n\nvLLM (THE PROFESSIONAL KITCHEN -- production standard):\nImagine a restaurant kitchen designed for maximum throughput. Orders come in constantly. The kitchen handles multiple orders at once, reuses prepped ingredients, and never lets a burner sit idle.\nvLLM does this for LLMs: handles many requests simultaneously, reuses cached computations, and keeps the GPU busy 100% of the time.\nResult: 2-4x more orders served per hour compared to a basic kitchen.\nUse for: any production deployment. THE default choice.\n\nTGI (Text Generation Inference -- the Docker-friendly kitchen):\nImagine a kitchen that comes in a shipping container. Plug it in and it works. Built by HuggingFace. Great integration with their model hub.\nUse for: quickly deploying HuggingFace models when you want Docker simplicity.\n\nTENSORRT-LLM (NVIDIA's Formula 1 kitchen):\nImagine a kitchen built by the company that makes the ovens (NVIDIA). They know every trick to squeeze maximum performance from their hardware. 30-50% faster than vLLM on the same ovens.\nBut: complex to set up. Like a Formula 1 car -- fastest but needs a pit crew.\nUse for: when every millisecond and every request-per-second matters.\n\nOLLAMA (the home kitchen):\nImagine running a model on your laptop with ONE command: ollama run llama3\nPerfect for trying recipes at home (development, demos). NOT for a restaurant (production).\nUse for: local development, testing, demos.\n\nLLAMA.CPP (the camping stove -- works without electricity):\nImagine cooking without a gas line or electric stove. Just a portable burner.\nllama.cpp runs LLMs on your CPU -- no GPU needed! A 7B model runs on a laptop. A 70B model runs on a Mac with 64GB RAM.\nUse for: when you have no GPU at all. Edge devices. Offline scenarios.\n\nDECISION:\nProduction with GPUs -> vLLM (90% of cases)\nQuick Docker deploy -> TGI\nMaximum speed on NVIDIA -> TensorRT-LLM\nLocal development -> Ollama\nNo GPU available -> llama.cpp",tip:"Think of choosing an engine like choosing a vehicle for a delivery service. vLLM = delivery van (reliable, handles volume). TGI = rental van (quick to get). TensorRT = race car (fastest but needs a mechanic). Ollama = bicycle (personal use). llama.cpp = walking (works anywhere, no vehicle needed).\n\n1. vLLM is your default. Best balance of speed and ease of use.\n2. Do NOT use Ollama in production. It is designed for one person, not thousands.\n3. TensorRT-LLM: 30-50% faster but 10x harder to set up. Only worth it if throughput is your bottleneck.\n4. llama.cpp: surprisingly fast on Apple Silicon Macs (uses Metal GPU acceleration).\n5. LiteLLM: not an engine but a TRANSLATOR. It gives you ONE interface that talks to ANY engine. Switch engines without changing your code.\n6. ALL engines speak the same language (OpenAI-compatible API). Your client code works with any engine.\n\nWAR STORY: Team used raw HuggingFace transformers to serve their model. 15 requests per second. Switched to vLLM: 55 requests per second. Same model, same GPU. The only change was the kitchen -- the chef stayed the same.",pocket:"vLLM: professional kitchen. 2-4x throughput. PRODUCTION STANDARD.\nTGI: Docker-friendly HuggingFace kitchen. Quick deploy.\nTensorRT-LLM: NVIDIA Formula 1. 30-50% faster. Complex setup.\nOllama: home kitchen. One command. NOT for production.\nllama.cpp: camping stove. CPU only. No GPU needed.\nDecision: prod=vLLM | docker=TGI | max speed=TensorRT | local=Ollama | no GPU=llama.cpp\nLiteLLM: translator proxy. One interface for any engine.\nAll engines speak OpenAI-compatible API.\npip install vllm | ollama.com/download",interview:"SAY THIS: 'vLLM is my default for production. PagedAttention gives 2-4x throughput, continuous batching keeps GPUs busy, and the OpenAI-compatible API means zero client code changes. For local dev, Ollama.'\n\nQ: vLLM vs TGI?\nA: 'vLLM for maximum throughput and features like LoRA hot-swapping. TGI for quick Docker deployment of HuggingFace models. vLLM is generally faster.'\n\nQ: When would you use llama.cpp?\nA: 'No GPU available, edge devices, or offline scenarios. On Apple Silicon Macs it is surprisingly fast. Also for GGUF quantized models.'",code:"# vLLM: production serving (the professional kitchen)\nfrom vllm import LLM, SamplingParams\n\nllm = LLM(\n    model=\"meta-llama/Llama-3-8B-Instruct\",\n    gpu_memory_utilization=0.9,  # Use 90% of GPU\n    max_model_len=4096,\n)\n\nparams = SamplingParams(temperature=0.7, max_tokens=256)\noutputs = llm.generate([\"What is machine learning?\"], params)\nprint(outputs[0].outputs[0].text)\n\n# As OpenAI-compatible server (production):\n# python -m vllm.entrypoints.openai.api_server --model meta-llama/Llama-3-8B-Instruct --port 8000\n\n# Then use standard OpenAI client (your code does not change!):\nfrom openai import OpenAI\nclient = OpenAI(base_url=\"http://localhost:8000/v1\", api_key=\"dummy\")\nresp = client.chat.completions.create(\n    model=\"meta-llama/Llama-3-8B-Instruct\",\n    messages=[{\"role\":\"user\",\"content\":\"What is ML?\"}]\n)\nprint(resp.choices[0].message.content)\n\n# LiteLLM: one interface for ANY engine/provider\nimport litellm\nresp = litellm.completion(\n    model=\"ollama/llama3\",  # or gpt-4, claude-3, together_ai/...\n    messages=[{\"role\":\"user\",\"content\":\"Hello\"}]\n)\nprint(f\"LiteLLM: {resp.choices[0].message.content}\")",output:"ML is a subset of AI that enables systems to learn from data.\n\n# vLLM: 55 req/s vs HuggingFace 15 req/s. Same model. Same GPU.\n# Same OpenAI client code works with vLLM, TGI, or any engine.\n# LiteLLM: one interface for every provider. Switch = change one string."}
]},
{id:"opt",icon:"2",title:"Optimization Techniques",sub:"KV-cache, batching, speculative decoding, PagedAttention",intro:"Imagine a restaurant where the chef starts cooking every dish from scratch for every customer -- even if 10 people ordered the same pasta. Insane, right? You would prep the sauce in advance, cook multiple orders at once, and have a junior chef start plating while the senior chef finishes cooking. These 4 optimizations do exactly that for LLMs.",topics:[
{name:"KV-Cache + Continuous Batching + Speculative Decoding + PagedAttention",teach:"4 TRICKS THAT MAKE PRODUCTION SERVING 2-4X FASTER:\n\n1. KV-CACHE (remembering what you already read):\nImagine writing a long essay. Without KV-cache, every time you write a NEW word, you have to re-read the ENTIRE essay from the beginning to remember the context. For a 1000-word essay, writing word 1000 means re-reading all 999 previous words. Insanely slow.\n\nWith KV-cache: you REMEMBER what you already read. Each new word only needs to look at what is new, not re-read everything. Like having perfect memory instead of amnesia.\n\nResult: generating long responses goes from painfully slow to fast.\n\n2. CONTINUOUS BATCHING (no idle time):\nImagine a restaurant with 10 tables. Naive approach: wait until all 10 tables order, cook all 10 meals, serve all 10 at once. But table 1 orders a salad (fast) and table 10 orders a steak (slow). The salad customer waits for the steak to finish. GPU sits idle while the salad is ready.\n\nContinuous batching: as SOON as the salad is done, serve it AND take a new order for that slot. The kitchen never has an empty burner.\n\nResult: 2-3x more meals served per hour. The GPU is ALWAYS working.\n\n3. SPECULATIVE DECODING (the sous chef trick):\nImagine the head chef (big expensive model) is very careful and slow. A junior chef (small cheap model) is fast but sometimes wrong.\n\nSpeculative decoding: the junior chef quickly prepares 5 components. The head chef checks all 5 at once (much faster than doing them one by one). If the junior was right (usually 70-80% of the time), the head chef approves all 5 instantly. If wrong, the head chef corrects only the wrong parts.\n\nResult: 2-3x faster because the head chef verifies in bulk instead of doing everything from scratch.\n\n4. PAGEDATTENTION (vLLM's secret weapon):\nImagine a parking lot. Traditional approach: each car reserves a WHOLE ROW of spaces, even if they only need 3 spaces. Massive waste -- most rows are half-empty.\n\nPagedAttention: allocate spaces on demand, one at a time. No wasted spaces. Same parking lot fits 2-4x more cars.\n\nThis is specifically about how GPU memory is used for the KV-cache. Traditional = wasteful contiguous blocks. Paged = efficient on-demand pages. This is WHY vLLM beats other engines.\n\nAll 4 are built into vLLM. Use vLLM = get all 4 optimizations for free.",tip:"Think of these like restaurant efficiency upgrades:\n  KV-cache = prep station (do not re-chop vegetables for every dish)\n  Continuous batching = always have something on every burner\n  Speculative decoding = junior chef preps, senior chef approves in bulk\n  PagedAttention = flexible seating instead of fixed reserved tables\n\n1. You do NOT configure these manually. vLLM enables all 4 automatically.\n2. Speculative decoding: needs a good draft model. Llama-3-1B as draft for Llama-3-8B.\n3. Max_model_len: set this to the MINIMUM you need. Shorter context = more concurrent users.\n4. GPU memory utilization: vLLM default is 0.9 (90%). Push to 0.95 if the GPU is dedicated.\n\nWAR STORY: Team served a 70B model. Maximum 8 users at the same time. Switched to vLLM (PagedAttention): 32 concurrent users on the same GPU. 4x improvement from smarter memory management alone. No hardware upgrade needed.",pocket:"KV-Cache: remember what you already read. No re-reading entire essay.\nContinuous Batching: as one request finishes, slot in new one. GPU never idle. 2-3x.\nSpeculative Decoding: fast junior model proposes, slow senior verifies in bulk. 2-3x.\nPagedAttention: flexible memory allocation. 2-4x more concurrent users. vLLM exclusive.\nAll 4 built into vLLM. Use vLLM = get all free.\nMax_model_len: shorter = more concurrent requests.\nGPU utilization: 0.9 default, 0.95 if dedicated.\nDraft model for speculative: small model that matches the big one.",interview:"SAY THIS: 'Production inference combines four optimizations: KV-cache avoids redundant computation, continuous batching keeps GPUs fully utilized, speculative decoding gets 2-3x generation speedup, and PagedAttention enables 2-4x more concurrent requests. vLLM implements all four.'\n\nQ: How does speculative decoding maintain quality?\nA: 'The large model VERIFIES every token. Wrong tokens from the draft model get corrected. Output is mathematically identical to running the large model alone. Speed without quality loss.'\n\nQ: Why is PagedAttention a big deal?\nA: 'KV-cache for a 70B model can use 40GB per request. Traditional allocation wastes memory through fragmentation, limiting to 8 concurrent requests. PagedAttention eliminates fragmentation, fitting 32+ in the same memory.'",code:"# vLLM with all 4 optimizations (all automatic)\nfrom vllm import LLM, SamplingParams\n\nllm = LLM(\n    model=\"meta-llama/Llama-3-8B-Instruct\",\n    gpu_memory_utilization=0.9,  # Use 90% of GPU RAM\n    max_model_len=4096,          # Shorter = more concurrent users\n    # All optimizations enabled automatically:\n    # KV-cache: on\n    # Continuous batching: on\n    # PagedAttention: on (this is vLLM's core innovation)\n    # Speculative decoding (optional):\n    # speculative_model=\"meta-llama/Llama-3-1B\",\n    # num_speculative_tokens=5,\n)\n\nprompts = [\n    \"Explain quantum computing in one sentence.\",\n    \"What is the capital of France?\",\n    \"Write a haiku about coding.\",\n]\nparams = SamplingParams(temperature=0.7, max_tokens=100)\noutputs = llm.generate(prompts, params)\n\nfor i, output in enumerate(outputs):\n    print(f\"Prompt {i+1}: {output.outputs[0].text[:60]}...\")\n\nprint(f\"\\n3 prompts processed SIMULTANEOUSLY via continuous batching.\")\nprint(f\"PagedAttention: 2-4x more requests fit in same GPU memory.\")",output:"Prompt 1: Quantum computers use qubits that can exist in multiple states...\nPrompt 2: The capital of France is Paris...\nPrompt 3: Code flows like streams / Bugs hide in nested loops deep / Stack...\n\n3 prompts processed SIMULTANEOUSLY via continuous batching.\nPagedAttention: 2-4x more requests fit in same GPU memory.\n# All optimizations working automatically. Zero configuration needed."}
]},
{id:"quant",icon:"3",title:"Quantization",sub:"GPTQ, AWQ, GGUF, bitsandbytes — shrink models",intro:"Imagine a beautiful 4K movie that is 50GB. You want to watch it on your phone but only have 10GB of space. You compress it to a smaller file -- it looks 97% as good, and now it fits. Quantization does the same for LLM models: compresses them to use less GPU memory with minimal quality loss. A 70B model shrinks from 140GB to 35GB.",topics:[
{name:"4 Methods + Decision Guide",teach:"THE PROBLEM:\nLLM models store each of their billions of numbers as a 16-bit value (2 bytes). A 70 billion parameter model: 70B x 2 bytes = 140 GB. That needs 2 expensive A100 GPUs (80GB each).\n\nTHE FIX -- QUANTIZATION:\nCompress each number from 16 bits to 4 bits (half a byte). Same 70B model: 70B x 0.5 bytes = 35 GB. Fits on ONE A100! Quality loss: about 1-3%. Like the 4K-to-1080p compression -- you barely notice the difference.\n\n4 COMPRESSION METHODS:\n\nGPTQ (the GPU standard):\nImagine compressing a movie with a smart algorithm that preserves the important scenes in high quality and compresses the boring parts more aggressively. GPTQ analyzes which weights matter most and preserves those.\nUse for: serving on GPUs. Widely supported by vLLM, TGI.\n\nAWQ (the quality-focused option):\nImagine a smarter version of GPTQ. It identifies which weights are ACTIVATED most often and preserves those with higher precision. Slightly better quality than GPTQ at the same compression level.\nUse for: when quality matters more than compatibility.\n\nGGUF (the CPU format):\nImagine a movie format optimized for playing on your phone instead of a TV. GGUF is optimized for CPU and Apple Silicon. Used by llama.cpp and Ollama.\nComes in quality levels: Q2 (most compressed), Q4 (balanced), Q5, Q8 (least compressed).\nQ4_K_M is the sweet spot for most people.\nUse for: local inference, no GPU, Ollama.\n\nBITSANDBYTES (the training format):\nSpecifically designed for QLoRA training (not production serving).\nCompresses the model so you can TRAIN on it with limited memory.\nUse for: fine-tuning only. For serving, use GPTQ or AWQ.\n\nQUALITY VS SIZE:\n  16-bit: 100% quality, 140 GB for 70B model\n  8-bit:  99% quality, 70 GB\n  4-bit:  97% quality, 35 GB\n  3-bit:  93% quality, 26 GB (usually too much loss)\n\nGOLDEN RULE:\nA quantized BIG model beats a full-precision SMALL model.\n70B at 4-bit (97% quality of a genius) > 13B at full precision (100% quality of a mediocre thinker).\nAlways quantize the BIGGEST model that fits your hardware.",tip:"Think of it like choosing between a compressed 4K movie on a big TV vs an uncompressed standard-definition movie on a small TV. The compressed 4K looks way better despite the compression.\n\n1. ALWAYS benchmark quantized vs full on YOUR data. The 1-3% quality loss varies by task.\n2. AWQ > GPTQ in quality at the same compression. Use AWQ when available.\n3. GGUF Q4_K_M: best balance for local/CPU inference.\n4. Do NOT compress below 4-bit for production. Quality drops too steeply.\n5. Pre-quantized models on HuggingFace: search for TheBloke or unsloth repos. Do NOT quantize yourself unless you have a specific reason.\n6. The BIGGEST mistake: running a full-precision small model instead of a quantized large model.\n\nWAR STORY: Team ran full-precision 13B model on an expensive A100. Competitor ran quantized 70B (AWQ 4-bit) on the same GPU. The competitor's model was dramatically better because a 97%-quality genius beats a 100%-quality average thinker.",pocket:"GPTQ: GPU production. Smart compression. Widely supported.\nAWQ: GPU production. Better quality than GPTQ at same size.\nGGUF: CPU/Ollama format. Q4_K_M best balance.\nbitsandbytes: for QLoRA TRAINING, not production serving.\nQuality: 16bit=100% | 8bit=99% | 4bit=97% | 3bit=93%\nSize: 70B fp16=140GB | 8bit=70GB | 4bit=35GB\nGolden rule: quantized 70B (97% genius) > full 13B (100% average)\nDecision: GPU prod=AWQ/GPTQ | CPU=GGUF Q4_K_M | training=bitsandbytes\nNever go below 4-bit in production.\nPre-quantized on HuggingFace: TheBloke, unsloth repos.",interview:"SAY THIS: 'I always quantize the largest model that fits. A 4-bit 70B significantly outperforms a full-precision 13B. For GPU production, AWQ for best quality. For local CPU, GGUF Q4_K_M. I always benchmark quantized vs full on my eval set.'\n\nQ: How much quality do you lose?\nA: 'Typically 1-3% on benchmarks. But it depends on the task -- some are more sensitive. I always test on MY eval set. If the drop exceeds 3%, I try 8-bit or find more GPU memory.'\n\nQ: When would you NOT quantize?\nA: 'When I have enough GPU memory for full precision and cost is not a concern. Or for tasks extremely sensitive to numerical precision. But in practice, quantization is almost always worth it.'",code:"# Quantization options\n\n# Option 1: Load pre-quantized GPTQ model in vLLM\nfrom vllm import LLM\nllm = LLM(\n    model=\"TheBloke/Llama-2-70B-Chat-GPTQ\",\n    quantization=\"gptq\",\n    tensor_parallel_size=1,  # Fits on 1 GPU now!\n)\nprint(\"70B GPTQ: fits on 1 GPU (was 2 at full precision)\")\n\n# Option 2: bitsandbytes 4-bit for QLoRA training\nfrom transformers import AutoModelForCausalLM, BitsAndBytesConfig\nbnb_config = BitsAndBytesConfig(\n    load_in_4bit=True,\n    bnb_4bit_quant_type=\"nf4\",\n    bnb_4bit_compute_dtype=\"bfloat16\",\n    bnb_4bit_use_double_quant=True,\n)\nmodel = AutoModelForCausalLM.from_pretrained(\n    \"meta-llama/Llama-3-70B\",\n    quantization_config=bnb_config, device_map=\"auto\")\nprint(\"BnB 4-bit: 70B loaded for QLoRA training\")\n\n# Option 3: GGUF via Ollama (one command!)\n# ollama run llama3:70b-q4_K_M\nprint(\"\\nGGUF: ollama run llama3:70b-q4_K_M (runs on CPU!)\")\n\nprint(\"\\n70B MODEL MEMORY:\")\nprint(\"  Full (16-bit):  140 GB (2 expensive GPUs)\")\nprint(\"  8-bit:           70 GB (1 expensive GPU)\")\nprint(\"  4-bit:           35 GB (1 GPU or 2 consumer GPUs)\")\nprint(\"  Quality loss:    ~1-3% at 4-bit. Barely noticeable.\")",output:"70B GPTQ: fits on 1 GPU (was 2 at full precision)\nBnB 4-bit: 70B loaded for QLoRA training\nGGUF: ollama run llama3:70b-q4_K_M (runs on CPU!)\n\n70B MODEL MEMORY:\n  Full (16-bit):  140 GB (2 expensive GPUs)\n  8-bit:           70 GB (1 expensive GPU)\n  4-bit:           35 GB (1 GPU or 2 consumer GPUs)\n  Quality loss:    ~1-3% at 4-bit. Barely noticeable."}
]},
{id:"gateway",icon:"4",title:"API Gateway Patterns",sub:"Fallbacks, load balancing, retries",intro:"Imagine you order all your restaurant supplies from one distributor. They have a truck breakdown. Your restaurant shuts down. Stupid, right? You should have a backup distributor. API gateway patterns = having multiple LLM providers, automatic failover, and smart retry logic so your app NEVER goes down because one provider has an issue.",topics:[
{name:"Multi-Provider + Fallbacks + Rate Limiting + Retries",teach:"4 PATTERNS (think of it as supply chain management for AI):\n\n1. MULTI-PROVIDER (do not put all eggs in one basket):\nImagine having 3 food suppliers. If Supplier A runs out of tomatoes, you automatically order from Supplier B.\n\nRoute across OpenAI + Anthropic + your own local model. If OpenAI goes down, traffic automatically shifts. Your users never notice.\n\n2. FALLBACK CHAINS (Plan A, Plan B, Plan C):\nPrimary: GPT-4o (best quality)\nFallback 1: Claude 3.5 (if OpenAI fails)\nFallback 2: local Llama-3-70B (if ALL cloud APIs fail)\n\nAutomatic failover. Like having a generator when the power goes out.\n\n3. RATE LIMITING (do not eat the whole buffet):\nOpenAI limits you to X requests per minute. Hit the limit = 429 error = your app crashes.\n\nSolution: queue requests, spread across providers, and limit per user (20 requests/minute) to prevent one user from eating your entire quota.\n\n4. RETRY WITH EXPONENTIAL BACKOFF (try again, but wait longer each time):\nRequest fails? Wait 1 second, try again.\nFails again? Wait 2 seconds, try again.\nFails again? Wait 4 seconds, try again.\nStill fails? Switch to fallback provider.\n\nAdd a little randomness (jitter) to each wait. Otherwise, 1000 users all retry at the exact same second and crash the server again (thundering herd).\n\nLITELLM:\nA Python proxy that translates between 100+ LLM providers. ONE interface, ANY provider.\nBuilt-in: fallbacks, load balancing, budget tracking, caching.\nLike having a universal power adapter that works in every country.",tip:"Think of it like building a reliable postal service. You do not use ONE delivery company. You have a primary (FedEx), backup (UPS), and emergency (your own van). Packages always arrive.\n\n1. ALWAYS configure at least 2 providers. Primary + fallback.\n2. LiteLLM is the standard proxy for multi-provider routing.\n3. Exponential backoff: 1s -> 2s -> 4s with random jitter. Max 3 retries.\n4. CACHE identical requests in Redis. If 100 people ask the same question, call the LLM once.\n5. HEALTH CHECKS: ping each provider every 30 seconds. Remove unhealthy ones from rotation.\n6. LOG which provider served each request. Essential for cost analysis and incident review.\n\nWAR STORY: Production app used ONLY OpenAI. OpenAI had a 4-hour outage. App was completely down. Lost $200K in revenue. Added Anthropic as fallback + local Llama as emergency. Next OpenAI outage: zero downtime. Fallback kicked in within 2 seconds.",pocket:"Multi-provider: do not depend on one. Route across OpenAI + Anthropic + local.\nFallback chain: Plan A -> Plan B -> Plan C -> error.\nRate limiting: per-user (20/min) + global (stay under provider limits).\nRetry: exponential backoff (1s, 2s, 4s) + random jitter + max 3 tries.\nLiteLLM: universal proxy for 100+ providers. Production standard.\n  Fallbacks, load balancing, budget tracking, caching.\nHealth checks: ping providers every 30s.\nCache identical requests (Redis) to save cost.\nALWAYS have 2+ providers configured.",interview:"SAY THIS: 'I design with multi-provider resilience. LiteLLM as gateway proxy, automatic failover chains, exponential backoff with jitter, and per-user rate limiting. Single provider outage = zero user impact.'\n\nQ: OpenAI goes down. What happens?\nA: 'Automatic failover to Anthropic within 2 seconds. If both down, route to self-hosted Llama. Users experience slightly different quality but zero downtime.'\n\nQ: How to handle rate limits?\nA: 'Three layers: per-user limit prevents abuse, global rate limiter stays under provider limits, and if approaching limits, queue requests rather than dropping them.'",code:"import litellm\nfrom litellm import completion\nimport time, random\n\n# Fallback chain: try providers in order\ndef resilient_call(messages):\n    models = [\"gpt-4o\", \"claude-3-5-sonnet-20241022\", \"ollama/llama3\"]\n    for i, model in enumerate(models):\n        try:\n            start = time.time()\n            response = completion(model=model, messages=messages, timeout=10)\n            ms = (time.time()-start)*1000\n            print(f\"  Provider {i+1} ({model}): OK in {ms:.0f}ms\")\n            return response\n        except Exception as e:\n            print(f\"  Provider {i+1} ({model}): FAILED\")\n            if i < len(models)-1: print(f\"  Falling back...\")\n    raise Exception(\"All providers failed\")\n\n# Exponential backoff with jitter\ndef retry_with_backoff(func, max_retries=3):\n    for attempt in range(max_retries):\n        try: return func()\n        except Exception as e:\n            if attempt == max_retries-1: raise\n            delay = (2**attempt) + random.uniform(0, 0.5)\n            print(f\"  Retry {attempt+1} in {delay:.1f}s...\")\n            time.sleep(delay)\n\n# Rate limiter (token bucket)\nclass RateLimiter:\n    def __init__(self, rpm=20):\n        self.rpm = rpm; self.calls = []\n    def allow(self):\n        now = time.time()\n        self.calls = [t for t in self.calls if now-t < 60]\n        if len(self.calls) >= self.rpm: return False\n        self.calls.append(now); return True\n\nprint(\"RESILIENT CALL:\")\nmsg = [{\"role\":\"user\",\"content\":\"What is 2+2?\"}]\nresult = resilient_call(msg)\nprint(f\"  Answer: {result.choices[0].message.content}\")\n\nlimiter = RateLimiter(rpm=20)\nprint(f\"\\nRate limiter: {'Allowed' if limiter.allow() else 'Blocked'}\")",output:"RESILIENT CALL:\n  Provider 1 (gpt-4o): OK in 850ms\n  Answer: 4\n\nRate limiter: Allowed\n\n# If GPT-4o fails: auto-fallback to Claude, then local Llama.\n# Backoff: 1s, 2s, 4s delays with jitter. No thundering herd.\n# Rate limiter: 20 requests per minute per user."}
]},
{id:"route",icon:"5",title:"Model Routing",sub:"Cheap model for simple, expensive for complex",intro:"Imagine a hospital emergency room. A patient with a paper cut does not need the head surgeon -- a nurse can handle it. A patient with a heart attack needs the best doctor available. Model routing = sending simple questions to cheap/fast models and complex questions to expensive/smart models. This cuts costs 50-70% with zero quality loss on simple queries.",topics:[
{name:"Complexity-Based Routing + Cascading",teach:"THE IDEA:\nNot every question needs the smartest (most expensive) model.\n\n'What time is it?' -> GPT-3.5 at $0.001/call (nurse handles it)\n'Design a microservices architecture for...' -> GPT-4 at $0.030/call (surgeon needed)\n\nIf 70% of your questions are simple, routing saves 70% x ($0.030 - $0.001) = massive savings.\n\n2 APPROACHES:\n\n1. COMPLEXITY ROUTER (classify first, then route):\nImagine a hospital receptionist who asks 'what is your problem?' and sends you to the right department.\n\nA small classifier checks each question: simple or complex?\n  Simple -> cheap model (GPT-3.5, fast and cheap)\n  Complex -> expensive model (GPT-4, smart and thorough)\n\nThe classifier can be: keyword rules ('compare', 'analyze', 'design' = complex), query length (longer = usually more complex), or a small ML model.\n\n2. CASCADING (try cheap first, escalate if unsure):\nImagine a customer service center. Junior agent answers first. If they are unsure, they transfer to a senior agent.\n\nStep 1: Send to GPT-3.5.\nStep 2: Check confidence. Does the answer contain hedging words ('maybe', 'I think', 'not sure')?\nStep 3: If low confidence -> re-send the SAME question to GPT-4.\n\nMost questions get answered by the junior agent. Only the tricky ones escalate. The user sees ONE answer -- they never know about the routing.\n\nCOST MATH:\n1000 queries/day, ALL to GPT-4: $30/day\n1000 queries/day, 70% to GPT-3.5 + 30% to GPT-4: $9.70/day\nSavings: 68%. Quality unchanged for the 70% simple queries.",tip:"Think of it like a law firm. The paralegal handles routine paperwork (cheap). The partner handles complex cases (expensive). You do not pay partner rates for filing paperwork.\n\n1. START SIMPLE: if query is shorter than 50 words, use cheap model. Iterate from there.\n2. Cascading is SAFER than routing because the cheap model tries first -- if it works, great. If not, the expensive model catches it.\n3. DOMAIN OVERRIDE: medical, legal, financial questions ALWAYS use the best model. Safety first. You do not route heart attacks to the nurse.\n4. A/B TEST your routing: randomly route 50% of traffic without routing, compare quality. Make sure routing does not hurt.\n\nWAR STORY: SaaS app routed ALL queries to GPT-4. $15K/month. Analysis showed 75% were simple lookups ('what is my plan?', 'how to reset password?'). Routed those to GPT-3.5. Cost dropped to $4K/month. Quality scores unchanged for simple queries.",pocket:"Complexity Router: classify question -> route to right model\nCascading: try cheap first -> escalate if uncertain\nAnalogy: hospital ER (nurse for paper cuts, surgeon for heart attacks)\nCost math: 70% routed to cheap = 68% savings\nClassify by: keywords, query length, small classifier model\nConfidence check: hedging words = escalate ('maybe', 'not sure')\nDomain override: medical/legal = ALWAYS best model\nStart: simple rules (query length). Iterate with data.\nA/B test routing to verify quality maintained.",interview:"SAY THIS: 'I implement cost-optimized model routing. Simple queries go to GPT-3.5, complex ones to GPT-4. For safety-critical domains, always the best model. This typically saves 50-70% with zero quality loss on simple queries.'\n\nQ: How do you build the classifier?\nA: 'Start with rules: query length, presence of reasoning keywords. Graduate to a fine-tuned classifier when you have production data. Train on 1000 labeled examples from production logs.'\n\nQ: What if cheap model gives a bad answer?\nA: 'Cascading: cheap answers first, I check for confidence signals. Low confidence triggers escalation. User sees one answer -- they never see the routing.'",code:"from openai import OpenAI\nclient = OpenAI()\n\ndef classify_complexity(query):\n    \"\"\"Simple classifier (upgrade to ML model in production)\"\"\"\n    complex_signals = [\"compare\",\"analyze\",\"design\",\"explain how\",\n                       \"tradeoffs\",\"step by step\",\"architecture\"]\n    if any(s in query.lower() for s in complex_signals): return \"complex\"\n    if len(query.split()) > 30: return \"complex\"\n    return \"simple\"\n\ndef routed_call(query):\n    complexity = classify_complexity(query)\n    model = \"gpt-3.5-turbo\" if complexity==\"simple\" else \"gpt-4\"\n    cost_rate = 0.001 if complexity==\"simple\" else 0.03\n    \n    r = client.chat.completions.create(\n        model=model,\n        messages=[{\"role\":\"user\",\"content\":query}])\n    tokens = r.usage.total_tokens\n    cost = tokens/1000 * cost_rate\n    print(f\"  [{complexity}] -> {model} | {tokens} tok | ${cost:.4f}\")\n    return r.choices[0].message.content\n\nprint(\"MODEL ROUTING:\")\nfor q in [\"What is 2+2?\",\n          \"What is the capital of France?\",\n          \"Compare PostgreSQL and MongoDB for a social media app\",\n          \"Design a microservices architecture for e-commerce\"]:\n    answer = routed_call(q)\n    print(f\"  Answer: {answer[:50]}...\\n\")",output:"MODEL ROUTING:\n  [simple] -> gpt-3.5-turbo | 45 tok | $0.0000\n  Answer: 4...\n\n  [simple] -> gpt-3.5-turbo | 38 tok | $0.0000\n  Answer: Paris...\n\n  [complex] -> gpt-4 | 320 tok | $0.0096\n  Answer: PostgreSQL is better for structured data with ACID...\n\n  [complex] -> gpt-4 | 450 tok | $0.0135\n  Answer: A microservices architecture for e-commerce should...\n\n# Simple questions: fast + cheap. Complex: smart + thorough."}
]},
{id:"stream",icon:"6",title:"Streaming + Containerization",sub:"SSE, Docker, Kubernetes",intro:"Without streaming, the user stares at a blank screen for 5 seconds, then the full answer appears at once. WITH streaming, the first word appears in 300ms and the rest flows in word by word. Users perceive streaming as 3-5x faster even though the total time is the same. Containerization (Docker/K8s) makes your deployment reproducible and scalable.",topics:[
{name:"SSE Streaming + Docker + K8s for LLM",teach:"STREAMING -- WHY USERS LOVE IT:\nImagine texting someone. You see the typing indicator and words appearing. Compare that to: silence for 30 seconds, then a wall of text. The first feels responsive. The second feels broken. Same total time, but perception is totally different.\n\nSSE (Server-Sent Events) -- THE STANDARD:\nThe server pushes words to the client one at a time over HTTP. Like a news ticker scrolling across a TV screen.\nAll LLM providers (OpenAI, Anthropic) use SSE for streaming. Set stream=True and you get tokens one by one.\n\nFIRST TOKEN LATENCY: The metric users actually FEEL. Target: under 500ms. This is how quickly the first word appears after the user hits send.\n\nCONTAINERIZATION -- WHY YOU NEED IT:\nImagine trying to recreate your kitchen setup in a new building. 'I used Python 3.11 with CUDA 12.1 and that specific version of PyTorch...' One wrong version and nothing works.\n\nDocker solves this: package your entire kitchen (code + dependencies + GPU drivers) into a shipping container. Ship it anywhere. It works the same everywhere.\n\nDOCKER FOR LLMS:\n- Use nvidia/cuda base image for GPU support\n- Pass GPUs with: docker run --gpus all\n- Health check endpoint: /health (loads model and runs one inference)\n- Do NOT bake model weights into the image (too big). Download at startup or mount from shared storage.\n\nKUBERNETES FOR SCALING:\nImagine you need 10 copies of your restaurant kitchen to handle dinner rush, but only 2 during lunch.\nK8s automatically scales: more copies when busy, fewer when quiet.\n- Autoscale on request QUEUE LENGTH (not CPU -- CPU is misleading for GPU workloads)\n- GPU node pools: machines with A100/H100 GPUs\n- Rolling updates: deploy new model version without downtime\n\nPRODUCTION STACK:\nvLLM (engine) -> Docker (package) -> Kubernetes (scale) -> Load Balancer -> LiteLLM (gateway) -> Client",tip:"Think of streaming like a waiter who brings courses as they are ready vs one who holds everything until the last course is done. Same total time. Completely different customer experience.\n\n1. ALWAYS enable streaming in production. Users perceive it as 3-5x faster.\n2. First token latency < 500ms is the target. This is what users feel.\n3. Docker image tip: do NOT put model weights in the image (would be 35GB+). Mount them from shared storage or download at startup.\n4. K8s autoscale on QUEUE LENGTH, not CPU. For GPU workloads, CPU is a misleading metric.\n5. Health check: do NOT just check if the port is open. Actually run a small inference. If the model is not loaded, the health check should fail.\n\nWAR STORY: Team deployed with 5-second average response time. Users complained 'it is slow.' Added streaming: same total time but first word appears in 300ms. User complaints dropped 90%. The food was not faster -- but the appetizer arrived immediately.",pocket:"Streaming:\n  SSE: server pushes words one at a time. THE standard.\n  First token latency < 500ms (what users FEEL).\n  Users perceive streaming as 3-5x faster (same total time).\n  stream=True in all provider APIs.\nDocker:\n  nvidia/cuda base image for GPU\n  --gpus all flag for GPU access\n  Do NOT put model weights in image (too big). Mount them.\n  Health check: /health that runs actual inference.\nKubernetes:\n  Autoscale on queue length, NOT CPU.\n  GPU node pools (A100, H100).\n  Rolling updates: zero-downtime model deploys.\nStack: vLLM -> Docker -> K8s -> Load Balancer -> LiteLLM -> Client",interview:"SAY THIS: 'I always deploy with streaming. First token latency under 500ms is the UX target. The stack is vLLM in Docker on Kubernetes, autoscaling on queue length, with LiteLLM as multi-provider gateway.'\n\nQ: How do you scale LLM on Kubernetes?\nA: 'GPU node pools with A100s. Autoscale on request queue length -- not CPU, which is misleading for GPU workloads. Rolling updates for model deploys. Health checks that run actual inference.'\n\nQ: Docker image is 15GB. How to reduce?\nA: 'Do not include model weights in the image. Download at startup or mount from shared storage. Multi-stage builds for code. Slim CUDA base images.'",code:"# FastAPI + SSE Streaming\nfrom fastapi import FastAPI\nfrom fastapi.responses import StreamingResponse\nfrom openai import OpenAI\nimport json\n\napp = FastAPI()\nclient = OpenAI()\n\n@app.get(\"/health\")\nasync def health():\n    return {\"status\": \"healthy\"}\n\n@app.post(\"/chat\")\nasync def chat(query: str):\n    async def stream_tokens():\n        response = client.chat.completions.create(\n            model=\"gpt-4\",\n            messages=[{\"role\":\"user\",\"content\":query}],\n            stream=True  # Words flow one at a time\n        )\n        for chunk in response:\n            if chunk.choices[0].delta.content:\n                token = chunk.choices[0].delta.content\n                yield f\"data: {json.dumps({'token': token})}\\n\\n\"\n        yield \"data: [DONE]\\n\\n\"\n    \n    return StreamingResponse(stream_tokens(), media_type=\"text/event-stream\")\n\nprint(\"\"\"# Dockerfile for LLM service:\nFROM nvidia/cuda:12.1.0-runtime-ubuntu22.04\nRUN pip install vllm fastapi uvicorn\nHEALTHCHECK CMD curl -f http://localhost:8000/health\nCMD [\"python\", \"-m\", \"vllm.entrypoints.openai.api_server\",\n     \"--model\", \"meta-llama/Llama-3-8B-Instruct\", \"--port\", \"8000\"]\n\n# Run: docker run --gpus all -p 8000:8000 llm-service\n# K8s: autoscale on queue length with GPU node pools\"\"\")",output:"# Dockerfile for LLM service:\nFROM nvidia/cuda:12.1.0-runtime-ubuntu22.04\nRUN pip install vllm fastapi uvicorn\nHEALTHCHECK CMD curl -f http://localhost:8000/health\nCMD [\"python\", \"-m\", \"vllm.entrypoints.openai.api_server\"...]\n\n# Run: docker run --gpus all -p 8000:8000 llm-service\n# K8s: autoscale on queue length with GPU node pools\n\n# Streaming: first word in <500ms. Users love it.\n# Without streaming: 5 seconds of nothing. Users hate it."}
]},
{id:"managed",icon:"7",title:"Serverless / Managed Platforms",sub:"Bedrock, Azure OpenAI, Groq, Together AI",intro:"Imagine the choice between building your own kitchen (self-hosted) vs ordering from restaurants (managed platforms). Building your own kitchen gives maximum control but needs equipment, maintenance, and a chef. Ordering from restaurants is easier but you are at their mercy for menu, pricing, and quality. Know when each makes sense.",topics:[
{name:"8 Platforms Compared",teach:"8 RESTAURANTS (managed platforms) vs COOKING AT HOME (self-hosted):\n\nAWS BEDROCK (Amazon's restaurant):\nMultiple chefs: Claude, Llama, Mistral, Titan. Fits into your existing AWS infrastructure.\nBest for: teams already on AWS.\n\nAZURE OPENAI (Microsoft's restaurant):\nOpenAI's models with Microsoft's security and compliance. Your data stays in your Azure region.\nBest for: enterprise on Azure who need OpenAI with compliance.\n\nGCP VERTEX AI (Google's restaurant):\nGemini models + open-source options. Google Cloud integration.\nBest for: teams already on Google Cloud.\n\nGROQ (the speed restaurant):\nCustom hardware (LPU) that serves 10-50x FASTER than GPU-based options. The food arrives almost instantly.\nLimited menu (fewer models). But incredibly fast.\nBest for: real-time apps where response time under 100ms matters.\n\nTOGETHER AI (the budget restaurant with great food):\nOpen-source models at low prices. You can fine-tune AND the model is yours to keep.\nBest for: cost-sensitive teams wanting open models.\n\nFIREWORKS AI (similar to Together):\nFast inference, good function calling support.\n\nREPLICATE (the food truck):\nRun any model in a container. Pay per second.\nBest for: experimentation, running unusual models.\n\nMODAL (the catering service):\nServerless GPU functions. Pay per second. Good for batch jobs.\nBest for: intermittent workloads that do not run 24/7.\n\nWHEN MANAGED vs SELF-HOSTED:\nManaged is cheaper when: under 1 million words/day (no idle GPU costs).\nSelf-hosted is cheaper when: over 1 million words/day (GPU runs constantly anyway).\nManaged is required when: using proprietary models (GPT-4, Claude API).\nSelf-hosted is required when: data cannot leave your servers (healthcare, finance, government).\n\nLOCK-IN PREVENTION:\nUse LiteLLM as your gateway proxy. Your code calls LiteLLM. LiteLLM calls the provider. Switch providers = change one config line, zero code changes.",tip:"Think of it like choosing between meal delivery, restaurants, and cooking at home. Delivery (managed) is convenient for occasional meals. Restaurants (specialized platforms) for special occasions. Cooking at home (self-hosted) saves money when you cook every day.\n\n1. Use YOUR CLOUD's offering: AWS=Bedrock, Azure=Azure OpenAI, GCP=Vertex. Simplest integration.\n2. Groq: incredible speed but limited models. Perfect for real-time chat.\n3. Together AI: best value for open-source models you can own.\n4. LiteLLM proxy: prevents vendor lock-in. One interface, any provider.\n5. Cost crossover: managed cheaper at < 1M tokens/day. Self-hosted cheaper above that.\n6. PROVISIONED THROUGHPUT (Azure, Bedrock): predictable pricing for stable workloads. Like a fixed monthly subscription vs pay-per-use.\n\nWAR STORY: Startup used Groq for ultra-fast chat (200ms responses). Users loved the speed. But Groq did not have their largest model. Solution: Groq for interactive chat, Together AI for background processing. LiteLLM routed automatically based on task type. Best of both worlds.",pocket:"AWS Bedrock: multi-model, AWS-native. Enterprise compliance.\nAzure OpenAI: OpenAI + Azure security. Data stays in your region.\nGCP Vertex: Gemini + open models. GCP-native.\nGroq: FASTEST (10-50x). Custom hardware. Limited models.\nTogether AI: open-source, cheap, you own the model.\nFireworks: fast open-source, function calling.\nReplicate: any model in container. Pay per second.\nModal: serverless GPU. Batch/intermittent workloads.\nDecision: use your cloud (AWS=Bedrock, Azure=AzureOAI, GCP=Vertex)\n  Speed=Groq | Budget=Together | Experimental=Replicate\nCrossover: managed cheaper <1M tok/day, self-host cheaper above.\nLiteLLM: prevents lock-in. One interface, any provider.",interview:"SAY THIS: 'I match platform to cloud infrastructure: Bedrock for AWS, Azure OpenAI for Azure. For speed-critical: Groq. For cost-sensitive open-source: Together AI. LiteLLM as abstraction layer to prevent lock-in.'\n\nQ: Managed vs self-hosted decision?\nA: 'Cost crossover at ~1M tokens/day. Below that, managed is cheaper. Above, self-hosted. Also: compliance requirements may mandate managed, and data privacy may mandate self-hosted.'\n\nQ: How to prevent lock-in?\nA: 'LiteLLM proxy. My code calls LiteLLM. LiteLLM calls the provider. Switching from OpenAI to Anthropic = one config change, zero code changes.'",code:"# Multi-platform with LiteLLM (one interface for everything)\nimport litellm\n\ndef call_any(model, prompt):\n    r = litellm.completion(\n        model=model,\n        messages=[{\"role\":\"user\",\"content\":prompt}])\n    return r.choices[0].message.content\n\n# Same interface, ANY provider:\n# call_any(\"gpt-4o\", \"Hello\")                     # OpenAI\n# call_any(\"claude-3-5-sonnet-20241022\", \"Hello\")  # Anthropic\n# call_any(\"bedrock/anthropic.claude-3\", \"Hello\")  # AWS Bedrock\n# call_any(\"azure/gpt-4\", \"Hello\")                 # Azure OpenAI\n# call_any(\"together_ai/meta-llama/Llama-3\", \"Hi\") # Together AI\n# call_any(\"groq/llama-3.1-70b-versatile\", \"Hi\")   # Groq (fastest)\n# call_any(\"ollama/llama3\", \"Hello\")               # Local Ollama\n\nresult = call_any(\"gpt-4o\", \"What is 2+2?\")\nprint(f\"Result: {result}\")\n\nprint(\"\\nPLATFORM GUIDE:\")\nfor name, best in [\n    (\"AWS Bedrock\",\"AWS enterprise\"),\n    (\"Azure OpenAI\",\"Azure compliance\"),\n    (\"Groq\",\"Ultra-low latency\"),\n    (\"Together AI\",\"Cheap open-source\"),\n    (\"Self-hosted vLLM\",\">1M tok/day\")]:\n    print(f\"  {name:20s} | Best for: {best}\")",output:"Result: 4\n\nPLATFORM GUIDE:\n  AWS Bedrock          | Best for: AWS enterprise\n  Azure OpenAI         | Best for: Azure compliance\n  Groq                 | Best for: Ultra-low latency\n  Together AI          | Best for: Cheap open-source\n  Self-hosted vLLM     | Best for: >1M tok/day\n\n# ONE interface for every platform. Switch = change one string.\n# Like a universal remote that works with any TV brand."}
]}
];
const L=[{n:"vllm",u:"Production inference engine",i:"pip install vllm"},{n:"litellm",u:"Multi-provider proxy",i:"pip install litellm"},{n:"fastapi",u:"API + streaming",i:"pip install fastapi uvicorn"},{n:"ollama",u:"Local model runner",i:"ollama.com/download"},{n:"boto3",u:"AWS Bedrock SDK",i:"pip install boto3"},{n:"openai",u:"OpenAI/Azure client",i:"pip install openai"}];
const AN=D.flatMap(s=>s.topics.map(t=>({name:t.name,section:s.title,pocket:t.pocket,interview:t.interview})));

export default function App(){
  const[sec,setSec]=useState(D[0].id);
  const[top,setTop]=useState(0);
  const[openPanels,setOpenPanels]=useState({teach:true,code:true});
  const[mode,setMode]=useState("learn");
  const[search,setSearch]=useState("");
  
  const S=D.find(s=>s.id===sec);
  const T=S?.topics[top];
  
  const toggle=k=>setOpenPanels(p=>({...p,[k]:!p[k]}));

  // Filter for pocket/interview modes
  const filtered=search?AN.filter(a=>a.name.toLowerCase().includes(search.toLowerCase())||a.pocket.toLowerCase().includes(search.toLowerCase())):AN;

  const Panel=({id,label,icon,color,gradient,children})=>{
    const isOpen=openPanels[id]!==undefined?openPanels[id]:(id==="teach"||id==="code");
    return(
      <div style={{marginBottom:10,borderRadius:12,overflow:"hidden",border:"1px solid rgba(255,255,255,0.06)",background:"rgba(255,255,255,0.02)"}}>
        <button onClick={()=>toggle(id)} style={{width:"100%",padding:"12px 16px",border:"none",background:isOpen?`linear-gradient(135deg,${gradient})`:("rgba(255,255,255,0.03)"),color:isOpen?"#fff":color,fontSize:12,fontWeight:700,cursor:"pointer",textAlign:"left",fontFamily:"inherit",display:"flex",justifyContent:"space-between",alignItems:"center",letterSpacing:0.8,textTransform:"uppercase",transition:"all 0.2s"}}>
          <span>{icon} {label}</span>
          <span style={{fontSize:16,transition:"transform 0.3s",transform:isOpen?"rotate(180deg)":"rotate(0)"}}>{isOpen?"▾":"▸"}</span>
        </button>
        {isOpen&&<div style={{padding:"14px 16px",borderTop:"1px solid rgba(255,255,255,0.04)"}}>{children}</div>}
      </div>
    );
  };

  const Pre=({color,children})=>(
    <pre style={{fontSize:12,lineHeight:1.75,color:color||"#c9d1d9",whiteSpace:"pre-wrap",margin:0,fontFamily:"'SF Mono','Fira Code','Courier New',monospace",wordBreak:"break-word",letterSpacing:0.2}}>{children}</pre>
  );

  const NoteCard=({item,i,color,field})=>(
    <div style={{background:"rgba(255,255,255,0.03)",border:"1px solid rgba(255,255,255,0.06)",borderRadius:12,marginBottom:12,overflow:"hidden",transition:"transform 0.2s"}}>
      <div style={{padding:"12px 16px",background:"rgba(255,255,255,0.04)",borderBottom:"1px solid rgba(255,255,255,0.05)",display:"flex",justifyContent:"space-between",alignItems:"center"}}>
        <span style={{fontSize:13,fontWeight:700,color}}>{i+1}. {item.name}</span>
        <span style={{fontSize:10,color:"#6e7681",background:"rgba(255,255,255,0.05)",padding:"3px 10px",borderRadius:20}}>{item.section}</span>
      </div>
      <div style={{padding:"14px 16px"}}>
        <Pre color={field==="pocket"?"#b5e8c3":"#d2a8ff"}>{item[field]}</Pre>
      </div>
    </div>
  );

  return(
    <div style={{fontFamily:"'Inter','SF Pro Display',-apple-system,system-ui,sans-serif",background:"#08080c",color:"#e0e0e0",minHeight:"100vh"}}>
      <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&family=Fira+Code:wght@400;500&display=swap" rel="stylesheet"/>
      
      {/* HEADER */}
      <div style={{background:"linear-gradient(135deg,#0f0a2e 0%,#0d1117 40%,#0a1a1a 100%)",padding:"24px 20px 20px",borderBottom:"1px solid rgba(255,255,255,0.06)"}}>
        <div style={{fontSize:11,fontWeight:600,color:"#39d353",textTransform:"uppercase",letterSpacing:2,marginBottom:6}}>Senior Architect Masterclass</div>
        <div style={{fontSize:26,fontWeight:900,background:"linear-gradient(135deg,#39d353,#a78bfa,#f472b6)",WebkitBackgroundClip:"text",WebkitTextFillColor:"transparent",lineHeight:1.2}}>Infrastructure</div>
        <div style={{fontSize:12,color:"#6e7681",marginTop:6}}>{D.reduce((a,s)=>a+s.topics.length,0)} Topics across {D.length} Sections — Learn, Revise, Interview</div>
      </div>

      {/* MODE TABS */}
      <div style={{display:"flex",gap:6,padding:"10px 16px",background:"rgba(255,255,255,0.01)",borderBottom:"1px solid rgba(255,255,255,0.04)"}}>
        {[["learn","Learn + Code","#39d353"],["pocket","Pocket Notes","#3fb950"],["interview","Interview Prep","#d2a8ff"]].map(([m,label,c])=>(
          <button key={m} onClick={()=>setMode(m)} style={{flex:1,padding:"10px",borderRadius:10,border:mode===m?`2px solid ${c}`:"2px solid transparent",background:mode===m?`${c}10`:"rgba(255,255,255,0.03)",color:mode===m?c:"#6e7681",fontSize:11,fontWeight:700,cursor:"pointer",fontFamily:"inherit",transition:"all 0.2s",letterSpacing:0.5}}>{label}</button>
        ))}
      </div>

      {/* POCKET / INTERVIEW MODES */}
      {mode!=="learn"&&(
        <div style={{padding:"16px"}}>
          <input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search topics..." style={{width:"100%",padding:"10px 14px",borderRadius:10,border:"1px solid rgba(255,255,255,0.08)",background:"rgba(255,255,255,0.03)",color:"#e0e0e0",fontSize:13,fontFamily:"inherit",marginBottom:14,outline:"none"}}/>
          <div style={{fontSize:12,color:"#6e7681",marginBottom:12}}>{filtered.length} topics</div>
          {filtered.map((item,i)=>(
            <NoteCard key={i} item={item} i={i} color={mode==="pocket"?"#3fb950":"#d2a8ff"} field={mode==="pocket"?"pocket":"interview"}/>
          ))}
        </div>
      )}

      {/* LEARN MODE */}
      {mode==="learn"&&(<>
        {/* SECTION NAV */}
        <div style={{display:"flex",gap:4,padding:"8px 12px",overflowX:"auto",borderBottom:"1px solid rgba(255,255,255,0.04)",background:"rgba(255,255,255,0.01)"}}>
          {D.map(s=>(
            <button key={s.id} onClick={()=>{setSec(s.id);setTop(0);setOpenPanels({teach:true,code:true});}} style={{padding:"8px 14px",borderRadius:20,border:sec===s.id?"1px solid #39d353":"1px solid transparent",background:sec===s.id?"rgba(88,166,255,0.1)":"transparent",color:sec===s.id?"#39d353":"#6e7681",fontSize:11,fontWeight:600,cursor:"pointer",whiteSpace:"nowrap",fontFamily:"inherit",transition:"all 0.2s"}}>{s.icon} {s.title}</button>
          ))}
        </div>

        {/* SECTION HEADER */}
        <div style={{padding:"16px 20px 8px"}}>
          <div style={{fontSize:20,fontWeight:800,color:"#e6edf3",letterSpacing:-0.3}}>{S?.title}</div>
          <div style={{fontSize:12,color:"#39d353",marginTop:3,fontWeight:500}}>{S?.sub}</div>
          {S?.intro&&(
            <div style={{fontSize:12,lineHeight:1.7,color:"#8b949e",marginTop:10,padding:"12px 16px",background:"rgba(255,255,255,0.02)",borderRadius:10,borderLeft:"3px solid #39d353"}}>
              {S.intro}
            </div>
          )}
        </div>

        {/* TOPIC SELECTOR */}
        {S?.topics.length>1&&(
          <div style={{display:"flex",gap:6,padding:"8px 20px 12px",flexWrap:"wrap"}}>
            {S.topics.map((t,i)=>(
              <button key={i} onClick={()=>{setTop(i);setOpenPanels({teach:true,code:true});}} style={{padding:"6px 16px",borderRadius:20,border:"none",background:top===i?"linear-gradient(135deg,#39d353,#a78bfa)":"rgba(255,255,255,0.04)",color:top===i?"#fff":"#8b949e",fontSize:11,fontWeight:top===i?700:500,cursor:"pointer",fontFamily:"inherit",transition:"all 0.2s"}}>{i+1}. {t.name}</button>
            ))}
          </div>
        )}

        {/* TOPIC CONTENT */}
        {T&&(
          <div style={{padding:"4px 20px 20px"}}>
            <div style={{background:"rgba(255,255,255,0.03)",borderRadius:12,padding:"14px 16px",marginBottom:12,borderLeft:"3px solid #39d353"}}>
              <div style={{fontSize:16,fontWeight:800,color:"#e6edf3"}}>{T.name}</div>
            </div>

            <Panel id="teach" label="Deep Teach" icon="📖" color="#a78bfa" gradient="#7c3aed,#a78bfa"><Pre>{T.teach}</Pre></Panel>
            {T.tip&&<Panel id="tip" label="Architect Tips + War Stories" icon="🏗" color="#f0883e" gradient="#ea580c,#f0883e"><Pre color="#f0883e">{T.tip}</Pre></Panel>}
            {T.pocket&&<Panel id="pocket" label="Pocket Notes" icon="📋" color="#3fb950" gradient="#16a34a,#3fb950"><Pre color="#3fb950">{T.pocket}</Pre></Panel>}
            {T.interview&&<Panel id="interview" label="Interview Prep" icon="🎯" color="#d2a8ff" gradient="#8b5cf6,#d2a8ff"><Pre color="#d2a8ff">{T.interview}</Pre></Panel>}
            
            <Panel id="code" label="Code" icon="⌨️" color="#3fb950" gradient="#166534,#238636">
              <div style={{background:"#0d1117",borderRadius:8,overflow:"hidden"}}>
                <div style={{padding:"8px 12px",background:"rgba(255,255,255,0.03)",borderBottom:"1px solid rgba(255,255,255,0.05)",display:"flex",alignItems:"center",gap:8}}>
                  <div style={{width:8,height:8,borderRadius:"50%",background:"#f85149"}}/>
                  <div style={{width:8,height:8,borderRadius:"50%",background:"#d29922"}}/>
                  <div style={{width:8,height:8,borderRadius:"50%",background:"#3fb950"}}/>
                  <span style={{fontSize:10,color:"#6e7681",marginLeft:8}}>Python</span>
                </div>
                <pre style={{padding:14,margin:0,overflow:"auto",fontSize:11.5,lineHeight:1.65,color:"#c9d1d9",fontFamily:"'Fira Code','SF Mono','Courier New',monospace",maxHeight:450}}><code>{T.code}</code></pre>
              </div>
            </Panel>

            {T.output&&<Panel id="output" label="Output" icon="📺" color="#d29922" gradient="#92400e,#d29922"><Pre color="#d29922">{T.output}</Pre></Panel>}
          </div>
        )}

        {/* LIBRARIES */}
        <div style={{margin:"0 20px 20px",background:"rgba(255,255,255,0.02)",border:"1px solid rgba(255,255,255,0.06)",borderRadius:12,padding:16}}>
          <div style={{fontSize:11,fontWeight:700,color:"#39d353",marginBottom:10,textTransform:"uppercase",letterSpacing:1.5}}>Libraries</div>
          {L.map((l,i)=>(
            <div key={i} style={{display:"flex",justifyContent:"space-between",alignItems:"center",padding:"8px 0",borderBottom:i<L.length-1?"1px solid rgba(255,255,255,0.04)":"none",flexWrap:"wrap",gap:6}}>
              <span><span style={{color:"#39d353",fontWeight:600,fontSize:12}}>{l.n}</span><span style={{color:"#6e7681",fontSize:11,marginLeft:8}}>{l.u}</span></span>
              <code style={{fontSize:10,color:"#7ee787",background:"rgba(63,185,80,0.1)",padding:"3px 10px",borderRadius:6,fontFamily:"'Fira Code',monospace"}}>{l.i}</code>
            </div>
          ))}
        </div>
      </>)}
    </div>
  );
}
