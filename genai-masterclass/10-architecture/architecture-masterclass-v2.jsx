import { useState } from "react";
const D=[
{id:"patterns",icon:"1",title:"Architectural Patterns",sub:"Gateway, Semantic Cache, Routing, Fallback, MapReduce, Refinement",intro:"Imagine building a restaurant empire. You do not just need great chefs (LLMs). You need a front desk that routes customers to the right restaurant (Gateway), a memory of what regulars order (Cache), backup kitchens when the main one is full (Fallback), and ways to handle a 500-person banquet by splitting it across locations (MapReduce). These 6 patterns are the blueprints every senior architect uses.",topics:[
{name:"Gateway Pattern",teach:"THE PROBLEM:\nImagine your app calls OpenAI, Anthropic, and your local Llama model. Each has a different API format, different error codes, different rate limits. Your code has 3 different client libraries, 3 different retry strategies, 3 different billing dashboards.\n\nTHE FIX -- GATEWAY PATTERN:\nImagine a hotel concierge. You tell the concierge what you need. They call the taxi company, the restaurant, the airline -- whoever is needed. You never talk to any of them directly. If the taxi company changes their phone number, the concierge updates their rolodex. You do not change anything.\n\nA Gateway sits between your application and ALL LLM providers:\n\nYour app -> Gateway -> OpenAI / Anthropic / Local Llama\n\nThe Gateway handles:\n  UNIFIED API: your app speaks ONE format. The gateway translates for each provider.\n  LOAD BALANCING: spread requests across providers to avoid rate limits.\n  FALLBACK: if OpenAI is down, automatically route to Anthropic.\n  OBSERVABILITY: one place to see ALL costs, latency, errors.\n  RATE LIMITING: protect against runaway costs.\n  CACHING: store repeated answers (save money).\n\nIMPLEMENTATION:\nLiteLLM is the standard gateway proxy. One interface for 100+ providers.\nSwitch providers = change one config string. Zero code changes.\n\nFor enterprise: deploy LiteLLM as a proxy server. All teams call the proxy.\nThis gives you centralized billing, usage tracking, and provider management.",tip:"Think of the Gateway like a universal power adapter. You carry ONE adapter that works in every country. Without it, you need a different plug for every country you visit.\n\n1. EVERY production LLM app should have a gateway. Even if you only use one provider today. You WILL add more.\n2. LiteLLM: the standard. pip install litellm. Works as a library or a proxy server.\n3. CENTRALIZE: one team manages the gateway. All other teams call it. This prevents 10 teams each managing their own OpenAI keys.\n4. The gateway is where you implement cost controls, usage quotas, and audit logging.\n5. TESTING: mock the gateway in tests. Your unit tests never call real LLMs.\n\nWAR STORY: Company had 8 teams each with their own OpenAI accounts. Total monthly bill: nobody knew (turned out to be $47K). Deployed a LiteLLM gateway. All teams call the gateway. Now: one dashboard, one bill, per-team quotas. Found 2 teams with runaway loops burning $8K/month on repeated identical calls. Gateway + caching saved $15K/month.",pocket:"Gateway: single entry point for all LLM providers.\nAnalogy: hotel concierge handles all external calls for you.\nHandles: unified API, load balancing, fallback, caching, billing.\nLiteLLM: standard gateway. 100+ providers. One interface.\n  Library mode: import litellm\n  Proxy mode: deploy as server, all teams call it.\nBenefits: centralized billing, usage quotas, audit logging.\nSwitch provider = change one config string.\nEvery production app should have a gateway.\npip install litellm",interview:"SAY THIS: 'I implement a Gateway pattern using LiteLLM as a centralized proxy. All teams call the gateway, which handles provider routing, fallback, rate limiting, and cost tracking. Switching providers is a config change, not a code change.'\n\nQ: Why not just call OpenAI directly?\nA: 'Three risks: vendor lock-in (switching requires rewriting code), no centralized cost control (runaway spend), and no fallback when OpenAI goes down. A gateway solves all three for minimal overhead.'\n\nQ: How do you manage costs across teams?\nA: 'Per-team API keys through the gateway with monthly quotas. Real-time dashboard showing spend by team, model, and endpoint. Alerts at 80% of budget.'",code:"import litellm\nfrom litellm import completion\nimport time\n\n# ONE interface for ANY provider\ndef gateway_call(prompt, provider=\"openai\"):\n    models = {\n        \"openai\": \"gpt-4o\",\n        \"anthropic\": \"claude-3-5-sonnet-20241022\",\n        \"local\": \"ollama/llama3\",\n    }\n    start = time.time()\n    r = completion(\n        model=models[provider],\n        messages=[{\"role\":\"user\",\"content\":prompt}]\n    )\n    ms = (time.time()-start)*1000\n    tokens = r.usage.total_tokens\n    print(f\"  [{provider}] {models[provider]} | {tokens} tok | {ms:.0f}ms\")\n    return r.choices[0].message.content\n\n# Same code, different providers\nprint(\"GATEWAY PATTERN:\")\nprint(gateway_call(\"What is 2+2?\", \"openai\"))\n# print(gateway_call(\"What is 2+2?\", \"anthropic\"))  # Same interface!\n# print(gateway_call(\"What is 2+2?\", \"local\"))       # Same interface!\n\n# As a proxy server (production):\nprint(\"\\n# Deploy as proxy:\")\nprint(\"# litellm --model gpt-4o --port 8000\")\nprint(\"# All teams call: http://gateway:8000/v1/chat/completions\")\nprint(\"# Standard OpenAI client format. Zero code changes.\")",output:"GATEWAY PATTERN:\n  [openai] gpt-4o | 28 tok | 650ms\n4\n\n# Deploy as proxy:\n# litellm --model gpt-4o --port 8000\n# All teams call: http://gateway:8000/v1/chat/completions\n# One gateway. All providers. Centralized billing."},
{name:"Semantic Cache",teach:"THE PROBLEM:\nImagine a customer asks 'What is your refund policy?' 500 times per day. You call the LLM 500 times, pay 500 times, and get the same answer 500 times. That is like driving to the grocery store every time you want a glass of water instead of using your fridge.\n\nEXACT MATCH CACHE (dumb fridge):\nStore: 'What is your refund policy?' -> answer.\nNext time the EXACT same question comes in, return the cached answer.\nProblem: 'What is the refund policy?' (without 'your') is a CACHE MISS. Different string = different query.\n\nSEMANTIC CACHE (smart fridge):\nInstead of matching exact words, match MEANING.\n'What is your refund policy?' and 'How do returns work?' and 'Can I get my money back?' all MEAN roughly the same thing. A semantic cache understands this.\n\nHow it works:\n1. User asks a question.\n2. Convert the question to an embedding (list of numbers representing meaning).\n3. Search the cache for questions with SIMILAR embeddings (cosine similarity > 0.95).\n4. If found: return cached answer instantly. Save the LLM call.\n5. If not found: call the LLM, cache both the question embedding and the answer.\n\nTHRESHOLD: similarity > 0.95 is safe (questions really mean the same thing). 0.90 is risky (might match questions that are similar but not identical).\n\nCACHE INVALIDATION:\nThe hardest problem in computer science. When does the answer become stale?\n  TIME-BASED: expire after 24 hours.\n  EVENT-BASED: when the underlying document changes, invalidate related cache entries.\n  HYBRID: expire after 24 hours OR when data changes, whichever comes first.\n\nCOST SAVINGS:\nIf 40% of your queries are semantically similar to previous ones:\n  Without cache: 100K queries/day x $0.03 = $3,000/day\n  With semantic cache: 60K queries x $0.03 + 40K cached = $1,800/day\n  Savings: $1,200/day = $36K/month",tip:"Think of semantic cache like a smart receptionist who recognizes when two different questions are really asking the same thing. 'Where is the bathroom?' and 'Can you direct me to the restroom?' get the same answer without asking the manager again.\n\n1. START with exact match cache (Redis). Add semantic cache only if hit rate is below 30%.\n2. Similarity threshold: 0.95 is safe. Below 0.92 = too many false matches.\n3. SEPARATE caches for different use cases. A refund query cache should not match pricing queries.\n4. Cache the EMBEDDING alongside the answer. Avoids re-embedding the cached question.\n5. GPTCache: open-source semantic caching library. pip install gptcache.\n6. TTL (time-to-live): 24 hours for dynamic content, 7 days for static content.\n\nWAR STORY: Customer support bot: 100K queries/day. Analysis showed 45% were variations of the same 200 questions. Deployed semantic cache: hit rate 42%. Cost dropped from $3K/day to $1.7K/day. Latency for cached queries: 50ms instead of 2 seconds. Win-win: cheaper AND faster.",pocket:"Exact Match: same string -> same answer. Simple. Low hit rate.\nSemantic Cache: same MEANING -> same answer. Embedding similarity.\n  'refund policy?' matches 'how do returns work?' (same meaning)\nHow: question -> embed -> search cache (cosine > 0.95) -> hit or miss\nThreshold: 0.95 safe | 0.92 risky | below 0.90 too many false matches\nInvalidation: time-based (24h) + event-based (data changes)\nCost: 40% semantic hit rate saves ~40% of LLM spend\nGPTCache: open-source semantic cache. pip install gptcache.\nStart: exact match (Redis). Add semantic if hit rate < 30%.\nSeparate caches per use case.",interview:"SAY THIS: 'I implement tiered caching: exact match in Redis for identical queries, semantic cache with embedding similarity for paraphrased queries. Typical hit rates of 30-45% save significant cost and improve latency from 2 seconds to 50ms for cached responses.'\n\nQ: How do you prevent stale cache responses?\nA: 'Hybrid invalidation: time-based TTL plus event-driven invalidation when source documents change. For RAG systems, I version-tag cache entries with the document hash -- when docs update, related cache entries auto-expire.'\n\nQ: Similarity threshold -- how do you choose?\nA: 'Start at 0.95 (very safe). Measure false positive rate on 1000 queries. If zero false positives, gradually lower to 0.93. Below 0.92, I start seeing different questions matched incorrectly.'",code:"import numpy as np\nfrom openai import OpenAI\nimport hashlib, time\nclient = OpenAI()\n\nclass SemanticCache:\n    def __init__(self, threshold=0.95):\n        self.cache = []  # [{embedding, question, answer, timestamp}]\n        self.threshold = threshold\n        self.hits = 0\n        self.misses = 0\n    \n    def embed(self, text):\n        r = client.embeddings.create(model=\"text-embedding-3-small\", input=text)\n        return np.array(r.data[0].embedding)\n    \n    def cosine(self, a, b):\n        return np.dot(a, b) / (np.linalg.norm(a) * np.linalg.norm(b))\n    \n    def get(self, question):\n        q_emb = self.embed(question)\n        for entry in self.cache:\n            sim = self.cosine(q_emb, entry[\"embedding\"])\n            if sim >= self.threshold:\n                self.hits += 1\n                return entry[\"answer\"], sim\n        self.misses += 1\n        return None, 0\n    \n    def set(self, question, answer):\n        self.cache.append({\n            \"embedding\": self.embed(question),\n            \"question\": question,\n            \"answer\": answer,\n            \"timestamp\": time.time()\n        })\n\ncache = SemanticCache(threshold=0.93)\n\nqueries = [\n    \"What is your refund policy?\",\n    \"How do returns work?\",          # Semantic match!\n    \"Can I get my money back?\",       # Semantic match!\n    \"What is the Pro plan price?\",    # Different topic = cache miss\n]\n\nfor q in queries:\n    answer, sim = cache.get(q)\n    if answer:\n        print(f\"  CACHE HIT ({sim:.3f}): {q} -> {answer[:40]}...\")\n    else:\n        answer = f\"Simulated answer for: {q}\"\n        cache.set(q, answer)\n        print(f\"  CACHE MISS: {q} -> called LLM\")\n\nprint(f\"\\nHit rate: {cache.hits}/{cache.hits+cache.misses} = {cache.hits/(cache.hits+cache.misses)*100:.0f}%\")",output:"  CACHE MISS: What is your refund policy? -> called LLM\n  CACHE HIT (0.952): How do returns work? -> Simulated answer for: What is your...\n  CACHE HIT (0.938): Can I get my money back? -> Simulated answer for: What is your...\n  CACHE MISS: What is the Pro plan price? -> called LLM\n\nHit rate: 2/4 = 50%\n# 2 LLM calls saved. Same meaning = same answer. 50% cost reduction."},
{name:"Routing Pattern",teach:"THE PROBLEM:\nImagine a hospital where EVERY patient -- paper cut, broken arm, heart attack -- waits in the same line to see the same doctor. The heart attack patient waits 2 hours. The paper cut patient gets a $500 specialist visit. Terrible for everyone.\n\nTHE FIX -- ROUTING:\nA triage nurse at the entrance checks each patient:\n  Paper cut -> nurse (fast, cheap)\n  Broken arm -> regular doctor (medium)\n  Heart attack -> specialist (expensive, thorough)\n\nFor LLMs:\n  'What time do you close?' -> GPT-3.5 ($0.001) -- simple lookup\n  'Compare PostgreSQL and MongoDB' -> GPT-4 ($0.03) -- needs reasoning\n  'Analyze this legal contract' -> Claude with 200K context ($0.05) -- needs long context\n\n3 ROUTING STRATEGIES:\n\n1. RULE-BASED (the keyword triage nurse):\nSimple rules: if query contains 'compare', 'analyze', 'design' -> expensive model. If short and simple -> cheap model.\nPros: zero latency, zero cost, predictable.\nCons: misses nuance ('simple question about comparing prices' still routes to expensive).\n\n2. CLASSIFIER-BASED (the trained triage nurse):\nA small ML model (fine-tuned BERT or GPT-3.5) classifies each query into complexity levels.\nPros: much more accurate than rules.\nCons: adds 50-100ms latency and ~$0.0005 per classification.\n\n3. CASCADING (try cheap first, escalate if unsure):\nSend EVERY query to the cheap model first. Check if it is confident.\nConfident (no hedging words like 'maybe', 'I think')? Ship it.\nNot confident? Re-send to the expensive model.\n\nPros: catches everything, no misrouting.\nCons: uncertain queries cost 2x (cheap + expensive call).\n\nCOST IMPACT:\n1000 queries/day, ALL to GPT-4: $30/day\n1000 queries, 70% routed to GPT-3.5: $9.70/day\nSavings: 68% with zero quality loss on simple queries.",tip:"Think of routing like a postal service. First class (GPT-4) for important letters. Standard mail (GPT-3.5) for junk mail. You do not pay first-class rates for every flyer.\n\n1. Start with RULES. Simple and effective for 80% of cases.\n2. DOMAIN OVERRIDE: medical, legal, financial = ALWAYS the best model. No routing. Safety first.\n3. Cascading is the SAFEST approach. The cheap model tries first. If it fails, the expensive model catches it.\n4. A/B test your routing: randomly send 5% without routing, compare quality. Ensure routing does not hurt.\n5. Track routing decisions and outcomes. If the cheap model fails on a pattern, add it to the 'always expensive' rules.\n\nWAR STORY: SaaS app routed ALL queries to GPT-4. $15K/month. Logged 10,000 queries and classified them. 75% were simple lookups. Routed those to GPT-3.5. Cost: $4K/month. Quality scores unchanged for simple queries. The 25% complex queries still get GPT-4.",pocket:"Routing: classify input -> route to right model/chain.\nAnalogy: hospital triage nurse sending patients to right department.\n3 strategies:\n  Rule-based: keywords/length. Zero cost. Predictable.\n  Classifier: small ML model. +50ms, +$0.0005. More accurate.\n  Cascading: cheap first -> confident? ship it. Unsure? escalate.\nCost: 70% routed to cheap = ~68% savings.\nDomain override: medical/legal = ALWAYS best model.\nA/B test routing to verify quality maintained.\nTrack routing decisions + outcomes for improvement.",interview:"SAY THIS: 'I implement cost-optimized model routing. Simple queries to GPT-3.5, complex to GPT-4, long-context to Claude. Rule-based routing for quick wins, classifier-based for precision. Domain-critical queries always go to the best model regardless of complexity.'\n\nQ: How do you build the classifier?\nA: 'Start with keyword rules: query length, reasoning keywords. Graduate to a fine-tuned classifier trained on 1000 labeled production queries. The classifier itself runs on GPT-3.5 for pennies.'\n\nQ: What if cheap model gives a wrong answer?\nA: 'Cascading catches this. Cheap model answers first. I check confidence signals (hedging words, low logprobs). Low confidence triggers automatic escalation to GPT-4. User sees one answer.'",code:"import time\n\ndef route_query(query):\n    \"\"\"3-tier routing based on complexity\"\"\"\n    # Rule-based classification\n    complex_signals = [\"compare\",\"analyze\",\"design\",\"architecture\",\n                       \"tradeoffs\",\"step by step\",\"explain how\"]\n    \n    if len(query.split()) > 50:\n        return \"claude-long\", \"Long context needed\"\n    if any(s in query.lower() for s in complex_signals):\n        return \"gpt-4\", \"Complex reasoning\"\n    return \"gpt-3.5-turbo\", \"Simple query\"\n\ndef cost_estimate(model, tokens=500):\n    prices = {\"gpt-3.5-turbo\":0.001,\"gpt-4\":0.03,\"claude-long\":0.015}\n    return tokens/1000 * prices.get(model, 0.01)\n\nprint(\"ROUTING DECISIONS:\")\ntotal_cost = 0\nfor q in [\n    \"What time do you close?\",\n    \"Compare PostgreSQL vs MongoDB for social media\",\n    \"Analyze this 50-page contract and summarize key risks \" + \"x \"*50,\n    \"What is the Pro plan price?\",\n    \"Design a microservices architecture for e-commerce\",\n]:\n    model, reason = route_query(q)\n    cost = cost_estimate(model)\n    total_cost += cost\n    print(f\"  [{model:16s}] ${cost:.4f} | {reason:20s} | {q[:45]}...\")\n\nall_gpt4_cost = 5 * cost_estimate(\"gpt-4\")\nprint(f\"\\nRouted cost:   ${total_cost:.4f}\")\nprint(f\"All-GPT-4 cost: ${all_gpt4_cost:.4f}\")\nprint(f\"Savings: {(1-total_cost/all_gpt4_cost)*100:.0f}%\")",output:"ROUTING DECISIONS:\n  [gpt-3.5-turbo   ] $0.0005 | Simple query         | What time do you close?...\n  [gpt-4           ] $0.0150 | Complex reasoning    | Compare PostgreSQL vs MongoDB for social...\n  [claude-long     ] $0.0075 | Long context needed  | Analyze this 50-page contract and summar...\n  [gpt-3.5-turbo   ] $0.0005 | Simple query         | What is the Pro plan price?...\n  [gpt-4           ] $0.0150 | Complex reasoning    | Design a microservices architecture for ...\n\nRouted cost:   $0.0385\nAll-GPT-4 cost: $0.0750\nSavings: 49%"},
{name:"Fallback Chain + MapReduce + Iterative Refinement",teach:"3 MORE ESSENTIAL PATTERNS:\n\n1. FALLBACK CHAIN (Plan A, Plan B, Plan C):\nImagine a power grid. Your house normally gets electricity from the main grid. If the grid fails, your generator kicks in. If the generator fails, your battery backup takes over.\n\nPrimary: GPT-4o (best quality)\n  fails -> Fallback 1: Claude (second best)\n    fails -> Fallback 2: local Llama (always available)\n      fails -> Graceful degradation: 'I am temporarily unavailable. Please try again.'\n\nAutomatic. The user never sees the switching. They just get an answer.\nCRITICAL: test your fallback chain monthly. Like testing fire alarms.\n\n2. MAPREDUCE FOR LLMS (divide and conquer):\nImagine you need to summarize a 500-page book. The LLM can only read 50 pages at a time.\n\nMAP: Split the book into 10 chapters of 50 pages each. Summarize each chapter separately (in parallel!).\nREDUCE: Take all 10 chapter summaries and summarize THOSE into one final summary.\n\nThis works for: summarizing long documents, analyzing large codebases, processing many reviews, extracting data from many files.\n\nCost trick: MAP step can use cheap model (just extracting/summarizing). REDUCE step uses smart model (combining and reasoning).\n\n3. ITERATIVE REFINEMENT (write, critique, rewrite):\nImagine a writer who writes a first draft, then a harsh editor reads it and says 'this paragraph is vague, this section is too long, you missed the key point.' The writer rewrites. The editor checks again. After 3 rounds, the quality is excellent.\n\nLoop: Generate -> Critique -> Regenerate (with critique as feedback) -> repeat 2-3 times.\n\nBest for: reports, code generation, complex analysis.\nMax iterations: 3. Diminishing returns after that.\nThe CRITIC can be a different model, a rule-based checker, or even the same model with a 'critic' prompt.",tip:"These 3 patterns are like building a reliable delivery service:\n  Fallback = backup drivers when the main driver is sick\n  MapReduce = splitting a huge delivery into smaller trucks\n  Refinement = quality inspection before delivery\n\n1. FALLBACK: test monthly. An untested fallback is the same as no fallback.\n2. MAPREDUCE: run MAP steps in PARALLEL. 10 chapters at once = same time as 1 chapter.\n3. REFINEMENT: max 3 iterations. After 3, if it is still bad, the problem is the TASK not the iterations.\n4. Combine: MapReduce (split doc) + Refinement (critique each summary) = highest quality.\n5. Fallback latency: add 2-3 second timeout before switching. Do not wait forever for a dead provider.\n\nWAR STORY: Company used only OpenAI. OpenAI had a 4-hour outage. App completely down. $200K in lost revenue. Added Anthropic fallback + local Llama emergency. Next OpenAI outage: zero downtime. Fallback kicked in within 2 seconds. Users never noticed.",pocket:"Fallback Chain: GPT-4 -> Claude -> local Llama -> graceful error.\n  Automatic switching. User never notices. Test monthly.\n  Timeout: 2-3 seconds before switching to fallback.\nMapReduce: split big doc -> summarize each piece (parallel) -> combine.\n  MAP: cheap model (extract). REDUCE: smart model (synthesize).\n  Works for: long docs, codebases, batch analysis.\nIterative Refinement: generate -> critique -> regenerate. Max 3 rounds.\n  Best for: reports, code, complex analysis.\n  Critic: different model, rules, or same model with critic prompt.\nCombine: MapReduce + Refinement = highest quality on long docs.",interview:"SAY THIS: 'I design with three resilience patterns: fallback chains for provider outages, MapReduce for documents exceeding context limits, and iterative refinement for quality-critical outputs. Each addresses a different failure mode.'\n\nQ: OpenAI goes down. What happens?\nA: 'Automatic failover to Anthropic within 2 seconds. If both down, local Llama. Users experience slightly different response characteristics but zero downtime. I test the chain monthly.'\n\nQ: 500-page document. How to process?\nA: 'MapReduce: split into 50-page chunks, summarize each in parallel with GPT-3.5, combine summaries with GPT-4. Parallel MAP means 10 chunks take the same time as 1. Then iterative refinement on the final summary.'",code:"import asyncio\nfrom openai import OpenAI\nclient = OpenAI()\n\n# FALLBACK CHAIN\ndef fallback_call(prompt):\n    providers = [\n        (\"gpt-4o\", lambda: client.chat.completions.create(\n            model=\"gpt-4o\", messages=[{\"role\":\"user\",\"content\":prompt}], timeout=5)),\n        (\"gpt-3.5-turbo\", lambda: client.chat.completions.create(\n            model=\"gpt-3.5-turbo\", messages=[{\"role\":\"user\",\"content\":prompt}], timeout=5)),\n    ]\n    for name, call_fn in providers:\n        try:\n            r = call_fn()\n            print(f\"  [{name}] Success\")\n            return r.choices[0].message.content\n        except Exception as e:\n            print(f\"  [{name}] Failed: {e}. Trying next...\")\n    return \"Service temporarily unavailable.\"\n\n# MAPREDUCE\ndef map_reduce(chunks, map_prompt, reduce_prompt):\n    # MAP: summarize each chunk (could be parallel)\n    summaries = []\n    for i, chunk in enumerate(chunks):\n        r = client.chat.completions.create(\n            model=\"gpt-3.5-turbo\",  # Cheap for MAP\n            messages=[{\"role\":\"user\",\"content\":f\"{map_prompt}\\n{chunk}\"}]\n        )\n        summaries.append(r.choices[0].message.content)\n        print(f\"  MAP chunk {i+1}: {len(chunk)} chars -> {len(summaries[-1])} chars\")\n    \n    # REDUCE: combine summaries\n    combined = \"\\n\\n\".join(summaries)\n    r = client.chat.completions.create(\n        model=\"gpt-4\",  # Smart for REDUCE\n        messages=[{\"role\":\"user\",\"content\":f\"{reduce_prompt}\\n{combined}\"}]\n    )\n    print(f\"  REDUCE: {len(combined)} chars -> final summary\")\n    return r.choices[0].message.content\n\n# ITERATIVE REFINEMENT\ndef refine(prompt, max_rounds=3):\n    draft = client.chat.completions.create(\n        model=\"gpt-4\",\n        messages=[{\"role\":\"user\",\"content\":prompt}]\n    ).choices[0].message.content\n    \n    for i in range(max_rounds):\n        critique = client.chat.completions.create(\n            model=\"gpt-4\",\n            messages=[{\"role\":\"user\",\"content\":\n                f\"Critique this draft. List specific improvements:\\n{draft}\"}]\n        ).choices[0].message.content\n        \n        if \"no improvements\" in critique.lower(): break\n        \n        draft = client.chat.completions.create(\n            model=\"gpt-4\",\n            messages=[{\"role\":\"user\",\"content\":\n                f\"Improve based on feedback:\\nDraft:{draft}\\nFeedback:{critique}\"}]\n        ).choices[0].message.content\n        print(f\"  Refinement round {i+1} complete\")\n    return draft\n\nprint(\"FALLBACK:\")\nprint(fallback_call(\"What is 2+2?\"))",output:"FALLBACK:\n  [gpt-4o] Success\n4\n\n# MapReduce: split big docs, parallel summarize, combine.\n# Refinement: draft -> critique -> improve. Max 3 rounds.\n# Fallback: primary -> backup -> emergency -> graceful error."}
]},
{id:"design",icon:"2",title:"Design Considerations",sub:"Idempotency, async, circuit breaker, token budgets, caching",intro:"LLMs are fundamentally different from traditional APIs. They are slow (2-5 seconds), non-deterministic (same input = different output), expensive (per token), and unreliable (providers go down). These 5 design patterns handle those unique challenges. Think of them as the engineering rules for building with an unpredictable material.",topics:[
{name:"Non-Determinism + Async + Circuit Breaker + Token Budgets",teach:"5 DESIGN RULES FOR LLM SYSTEMS:\n\n1. IDEMPOTENCY AND NON-DETERMINISM:\nImagine asking a colleague 'what is the capital of France?' five times. A human might say 'Paris' every time. An LLM might say: 'Paris', 'The capital is Paris', 'Paris, France', 'It is Paris', 'Paris is the capital.'\n\nSame question, different wording every time. This is NON-DETERMINISM.\n\nFor production:\n  temperature=0: makes output MORE deterministic (but not perfectly).\n  Structured output (JSON mode, instructor): forces consistent FORMAT even if words vary.\n  Idempotency keys: if a request is retried, do not process it twice. If the user clicked 'send' and got a timeout, the retry should not create a duplicate order.\n\n2. ASYNC-FIRST DESIGN:\nImagine a restaurant where the waiter stands at the kitchen watching your food cook for 3 minutes instead of taking other orders. Wasteful! The waiter should take more orders while your food cooks.\n\nLLM calls take 2-5 seconds. If your server WAITS during each call, it can only handle a few users. With async, it takes more requests while waiting for LLM responses.\n\nasyncio + httpx or aiohttp. Your server handles 10-50x more concurrent users.\n\n3. CIRCUIT BREAKER:\nImagine a fuse in your house. When there is a power surge, the fuse BREAKS to protect your appliances. After the surge passes, you reset it.\n\nCircuit breaker for LLM APIs:\n  CLOSED (normal): requests flow through.\n  OPEN (provider is down): ALL requests immediately return fallback response. Do not even try the dead provider.\n  HALF-OPEN (testing recovery): send ONE request to test. If it works, go back to CLOSED.\n\nWithout circuit breaker: 1000 users all wait 30 seconds for a timeout from a dead provider. WITH circuit breaker: after 5 failures, immediately return fallback for the next 999 users.\n\n4. TOKEN BUDGET MANAGEMENT:\nImagine a restaurant with a maximum occupancy. You cannot let unlimited people in or the building violates safety codes.\n\nEvery LLM call has a context window limit (128K tokens for GPT-4o). You need to fit:\n  System prompt: ~500 tokens (fixed)\n  Retrieved context: ~3000 tokens (variable)\n  User message: ~200 tokens (variable)\n  Conversation history: ~2000 tokens (growing!)\n  Space for response: ~2000 tokens (reserved)\n  TOTAL BUDGET: 7700 tokens\n\nIf conversation history grows too long, COMPRESS it: summarize old messages. Like throwing away old receipts but keeping a summary of your spending.\n\n5. CACHING STRATEGIES (3 levels):\n  Level 1 -- EXACT MATCH (Redis): identical query -> cached answer. Fast, simple, limited.\n  Level 2 -- SEMANTIC CACHE (embeddings): similar meaning -> cached answer. Smarter, higher hit rate.\n  Level 3 -- KV-CACHE (inference engine): vLLM caches computed attention. Automatic, no code needed.",tip:"These 5 rules are like building codes for LLM applications. You CAN ignore them -- until your app falls over in production.\n\n1. ASYNC is non-negotiable. Synchronous LLM calls = your server handles 5 users instead of 500.\n2. CIRCUIT BREAKER: implement with tenacity library (Python). 5 failures in 60 seconds = open circuit.\n3. TOKEN BUDGET: plan your token allocation BEFORE writing code. Draw a pie chart.\n4. temperature=0 + structured output = most deterministic. Still not 100% -- always handle variation.\n5. Three caching levels are complementary. Use all three for maximum savings.\n\nWAR STORY: Startup launched with synchronous LLM calls. Works fine with 10 users. At 100 users, server threads exhausted. Response time: 30+ seconds. Switched to async: same server, 500 concurrent users, 2-second response time. The fix was 20 lines of code (async/await).",pocket:"5 Design Rules:\n  Non-determinism: temp=0 + structured output. Still handle variation.\n  Async: LLM calls are I/O-bound. async = 10-50x more concurrent users.\n  Circuit breaker: 5 failures -> stop trying -> fallback -> test recovery.\n  Token budget: plan allocation. System + context + history + response = total.\n  Caching: exact match (Redis) + semantic (embeddings) + KV-cache (engine).\nAsync: non-negotiable. Sync = 5 users. Async = 500 users.\nIdempotency: retry should not create duplicates.\nCircuit breaker: CLOSED (normal) -> OPEN (dead) -> HALF-OPEN (testing).\nToken budget: compress history when it grows too long.",interview:"SAY THIS: 'I design LLM systems async-first because calls are I/O-bound, with circuit breakers for provider resilience, explicit token budgets per request, and tiered caching. Non-determinism is managed through structured outputs and idempotency keys.'\n\nQ: LLM calls are slow. How do you handle scale?\nA: 'Async-first: every LLM call is awaited, not blocking. The server handles 500 concurrent users instead of 5. Plus streaming for perceived speed, semantic caching for repeated queries, and model routing so simple queries use fast models.'\n\nQ: Provider is down. Your circuit breaker triggers. Then what?\nA: 'Immediately return responses from fallback provider. No queue, no waiting. After 60 seconds, send ONE probe request to the primary. If it succeeds, reset the circuit. Log everything for post-incident review.'",code:"import asyncio, time\nfrom dataclasses import dataclass\n\n# CIRCUIT BREAKER\n@dataclass\nclass CircuitBreaker:\n    failures: int = 0\n    threshold: int = 5\n    state: str = \"closed\"  # closed, open, half-open\n    last_failure: float = 0\n    recovery_timeout: int = 60  # seconds\n    \n    def call(self, func, fallback):\n        if self.state == \"open\":\n            if time.time() - self.last_failure > self.recovery_timeout:\n                self.state = \"half-open\"\n                print(\"  Circuit: HALF-OPEN (testing...)\")\n            else:\n                print(\"  Circuit: OPEN -> immediate fallback\")\n                return fallback()\n        \n        try:\n            result = func()\n            if self.state == \"half-open\":\n                self.state = \"closed\"\n                self.failures = 0\n                print(\"  Circuit: recovered -> CLOSED\")\n            return result\n        except Exception:\n            self.failures += 1\n            self.last_failure = time.time()\n            if self.failures >= self.threshold:\n                self.state = \"open\"\n                print(f\"  Circuit: OPEN after {self.failures} failures\")\n            return fallback()\n\n# TOKEN BUDGET\ndef plan_token_budget(max_context=8192):\n    budget = {\n        \"system_prompt\": 500,\n        \"retrieved_docs\": 3000,\n        \"conversation_history\": 2000,\n        \"user_message\": 200,\n        \"response_reserved\": 2000,\n    }\n    total = sum(budget.values())\n    remaining = max_context - total\n    print(\"TOKEN BUDGET (8K context):\")\n    for name, tokens in budget.items():\n        pct = tokens/max_context*100\n        bar = '#' * int(pct/2)\n        print(f\"  {name:25s} {tokens:5d} tok ({pct:4.1f}%) {bar}\")\n    print(f\"  {'REMAINING':25s} {remaining:5d} tok ({remaining/max_context*100:.1f}%)\")\n\nplan_token_budget()\n\n# ASYNC example\nprint(\"\\nASYNC BENEFIT:\")\nprint(\"  Sync:  5 concurrent users  (thread per request)\")\nprint(\"  Async: 500 concurrent users (event loop)\")\nprint(\"  Same server. Same code. 100x more users.\")",output:"TOKEN BUDGET (8K context):\n  system_prompt               500 tok ( 6.1%) ###\n  retrieved_docs             3000 tok (36.6%) ##################\n  conversation_history       2000 tok (24.4%) ############\n  user_message                200 tok ( 2.4%) #\n  response_reserved          2000 tok (24.4%) ############\n  REMAINING                   492 tok ( 6.0%)\n\nASYNC BENEFIT:\n  Sync:  5 concurrent users  (thread per request)\n  Async: 500 concurrent users (event loop)\n  Same server. Same code. 100x more users."}
]},
{id:"data",icon:"3",title:"Data Architecture",sub:"Embedding pipelines, vector DB schema, hybrid storage, freshness",intro:"Imagine building a library from scratch. You need to decide: How do you organize the books when they arrive (embedding pipeline)? How do you arrange the shelves (vector DB schema)? Do you need BOTH a card catalog AND shelf organization (hybrid storage)? And when new editions come out, how do you replace old books (data freshness)? This is data architecture for GenAI.",topics:[
{name:"Embedding Pipeline Design",teach:"TWO PIPELINE MODES:\n\n1. BATCH PIPELINE (the overnight delivery truck):\nImagine a bookstore that receives a delivery truck of 10,000 new books every night. Staff processes them overnight: scan, catalog, shelve. Ready by morning.\n\nBatch embedding: run nightly. Process all new/changed documents at once.\n  Source (DB, S3, Drive) -> Extract text -> Chunk -> Embed -> Store in vector DB\n\nBest for: knowledge bases that update daily or weekly. Internal docs, product catalogs.\nCost: cheapest per document (bulk pricing).\nFreshness: up to 24 hours stale.\n\n2. REAL-TIME PIPELINE (the instant messenger):\nImagine a newsroom where breaking news must be searchable within seconds of publishing.\n\nReal-time embedding: process each document immediately when created/changed.\n  Document created -> webhook/event -> chunk -> embed -> store -> immediately searchable\n\nBest for: customer support (new tickets), news, chat history, real-time data.\nCost: higher per document (no bulk discount, more infrastructure).\nFreshness: seconds.\n\nHYBRID (the practical choice):\nBatch for the initial load + real-time for incremental updates.\nLike the bookstore: overnight truck for big shipments, bike messenger for urgent deliveries.\n\nPIPELINE COMPONENTS:\n1. EXTRACTOR: pull text from PDFs, DOCX, HTML, databases. Use Unstructured library.\n2. CHUNKER: split into 512-token pieces with 50-token overlap.\n3. EMBEDDER: OpenAI text-embedding-3-small or local model.\n4. LOADER: write to vector DB (Qdrant, Pinecone, pgvector).\n5. METADATA: attach source, date, author, document type. Essential for filtering.\n\nDEDUPLICATION:\nImagine the same document uploaded 3 times. Without dedup, you embed it 3 times (waste), store it 3 times (waste), and retrieve it 3 times (confuses the LLM with 3 identical chunks).\nHash each chunk content. Skip if hash already exists.",tip:"Think of the embedding pipeline like a mail processing center. Mail arrives (documents). It gets sorted (chunked), labeled (embedded), and filed (stored). The question is: do you process once a day (batch) or instantly (real-time)?\n\n1. START with batch. It is simpler, cheaper, and good enough for 80% of use cases.\n2. Add real-time only for content that MUST be searchable immediately.\n3. METADATA is critical. Always attach: source_url, date_created, document_type, chunk_index. You will need these for filtering and debugging.\n4. DEDUPLICATION: hash each chunk. Skip duplicates. Saves 10-30% of embedding costs.\n5. IDEMPOTENT: running the pipeline twice should produce the same result, not duplicate everything.\n6. MONITORING: track documents processed, errors, embedding latency, storage growth.\n\nWAR STORY: Team ran their embedding pipeline daily but had no deduplication. After 6 months, 40% of their vector DB was duplicates. Retrieval quality dropped because the LLM got 3 identical chunks instead of 3 diverse ones. Added content hashing: removed 40% of vectors, retrieval precision improved 15%.",pocket:"Batch: nightly processing. Cheapest. Up to 24h stale. 80% of use cases.\nReal-time: process on creation. Seconds freshness. More infrastructure.\nHybrid: batch for bulk + real-time for urgent. PRACTICAL CHOICE.\nPipeline: extract -> chunk -> embed -> store + metadata.\nMetadata: ALWAYS attach source, date, type, chunk_index.\nDedup: hash chunks. Skip duplicates. Saves 10-30%.\nIdempotent: run twice = same result (no duplicates).\nMonitor: docs processed, errors, latency, storage growth.\nStart batch. Add real-time only when freshness matters.",interview:"SAY THIS: 'I design embedding pipelines as batch-first with real-time supplements. Batch handles the bulk economically. Real-time webhooks handle urgent updates. Every chunk gets content-hashed for deduplication and tagged with metadata for filtering.'\n\nQ: How do you handle 1 million documents?\nA: 'Batch pipeline with parallelism. Split into 100 batches of 10K. Process 10 batches concurrently. Monitor: documents/minute, errors, embedding cost. Total: about 4-6 hours and $100 in embedding cost.'\n\nQ: Document updated. How quickly is it searchable?\nA: 'Depends on the mode. Batch: next nightly run (up to 24h). Real-time: webhook triggers immediate re-embedding, searchable in seconds. I choose based on business requirements.'",code:"import hashlib, time\nfrom dataclasses import dataclass\nfrom typing import List\n\n@dataclass\nclass Chunk:\n    text: str\n    metadata: dict\n    hash: str\n\nclass EmbeddingPipeline:\n    def __init__(self):\n        self.processed_hashes = set()  # Deduplication\n        self.stats = {\"processed\":0,\"skipped\":0,\"errors\":0}\n    \n    def chunk(self, text, chunk_size=500, overlap=50) -> List[str]:\n        words = text.split()\n        chunks = []\n        for i in range(0, len(words), chunk_size - overlap):\n            chunks.append(\" \".join(words[i:i+chunk_size]))\n        return chunks\n    \n    def process(self, doc_text, metadata):\n        chunks = self.chunk(doc_text)\n        results = []\n        for i, chunk_text in enumerate(chunks):\n            # Deduplication via content hash\n            h = hashlib.md5(chunk_text.encode()).hexdigest()\n            if h in self.processed_hashes:\n                self.stats[\"skipped\"] += 1\n                continue\n            self.processed_hashes.add(h)\n            \n            chunk = Chunk(\n                text=chunk_text,\n                metadata={**metadata, \"chunk_index\":i, \"hash\":h},\n                hash=h\n            )\n            results.append(chunk)\n            self.stats[\"processed\"] += 1\n        return results\n\npipeline = EmbeddingPipeline()\n\n# Process documents\ndocs = [\n    (\"Refund policy allows 30 days for returns. \" * 50, {\"source\":\"policy.pdf\",\"type\":\"policy\"}),\n    (\"Pro plan costs $49 per month. Enterprise $199. \" * 50, {\"source\":\"pricing.pdf\",\"type\":\"pricing\"}),\n    (\"Refund policy allows 30 days for returns. \" * 50, {\"source\":\"policy_v2.pdf\",\"type\":\"policy\"}),  # Duplicate!\n]\n\nfor text, meta in docs:\n    chunks = pipeline.process(text, meta)\n    print(f\"  {meta['source']}: {len(chunks)} chunks\")\n\nprint(f\"\\nStats: {pipeline.stats}\")\nprint(f\"Dedup saved {pipeline.stats['skipped']} redundant embeddings\")",output:"  policy.pdf: 2 chunks\n  pricing.pdf: 2 chunks\n  policy_v2.pdf: 0 chunks\n\nStats: {'processed': 4, 'skipped': 2, 'errors': 0}\nDedup saved 2 redundant embeddings\n\n# policy_v2.pdf was identical content -> 0 chunks processed. Money saved."},
{name:"Vector DB Schema + Hybrid Storage + Data Freshness",teach:"3 DATA ARCHITECTURE DECISIONS:\n\n1. VECTOR DB SCHEMA DESIGN:\nImagine organizing a library. You do not just throw books on random shelves. You organize by section (fiction, science), add labels (author, year), and create an index.\n\nVector DB schema:\n  Collection: 'support_docs' (like a database table)\n  Each record:\n    vector: [0.12, -0.34, ...] (the embedding)\n    text: 'Refund policy is 30 days...' (original content)\n    metadata:\n      source: 'policy.pdf'\n      doc_type: 'policy'\n      department: 'support'\n      date_updated: '2024-12-01'\n      chunk_index: 3\n\nMetadata filtering: 'Find chunks about refunds FROM the policy department UPDATED in the last 30 days.'\nThis pre-filters BEFORE vector search, making results much more relevant.\n\nMULTI-TENANT: separate collections per customer. Customer A cannot search Customer B's data.\n\n2. HYBRID STORAGE (relational + vector + document):\nImagine a business that needs THREE types of storage:\n  PostgreSQL (relational): customer accounts, orders, billing. Structured, transactional.\n  Qdrant (vector): document embeddings for RAG search. Semantic search.\n  S3/MongoDB (document): original PDF files, images, raw documents.\n\nEach stores what it is best at. They reference each other by ID.\nExample: user asks about their order. PostgreSQL finds order details. Vector DB finds relevant policy docs. S3 serves the original invoice PDF.\n\npgvector: PostgreSQL extension that adds vector search. One database for BOTH relational AND vector. Simpler architecture but slower than dedicated vector DBs at scale (10M+ vectors).\n\n3. DATA FRESHNESS AND RE-INDEXING:\nImagine your library has a policy book from 2022. The policy changed in 2024. If you do not replace the old book, customers get wrong answers.\n\nStrategies:\n  INCREMENTAL: only re-embed changed documents. Track changes via last_modified timestamp. Like replacing only the updated pages.\n  FULL RE-INDEX: re-embed everything. When switching embedding models or major restructuring. Like reorganizing the entire library.\n  VERSION TAGGING: keep old and new versions. Switch atomically. Like building a new shelf while the old one is still serving customers, then swapping overnight.\n\nRE-INDEX TRIGGERS:\n  - Embedding model change (MUST re-embed everything)\n  - Chunking strategy change (affects all chunks)\n  - Source document updates (incremental)\n  - Scheduled refresh (weekly/monthly full re-index for safety)",tip:"Think of data architecture like city planning. You need roads (relational DB for structured data), parks (vector DB for semantic search), and warehouses (document store for raw files). Each serves a purpose.\n\n1. METADATA is your secret weapon. Always store: source, date, type, department. Pre-filtering on metadata before vector search dramatically improves results.\n2. pgvector under 10M vectors: use it. One fewer database to manage. Above 10M: dedicated vector DB (Qdrant).\n3. MULTI-TENANT: separate collections per customer. Never mix customer data in one collection.\n4. VERSION your re-index: build new index alongside old. Swap atomically. Zero downtime.\n5. Monitor: vector count growth, query latency by collection size, stale document count.\n\nWAR STORY: RAG system answered 'refund policy is 14 days' when it had changed to 30 days 3 months ago. The old document was still in the vector DB. No freshness mechanism. Added: change detection via last_modified, weekly incremental re-index, and a 'freshness score' that penalizes old documents in ranking. Stale answer rate dropped from 12% to 0.5%.",pocket:"Vector DB Schema:\n  Collection per domain (support_docs, product_docs)\n  Each record: vector + text + metadata (source, date, type, department)\n  Metadata filtering BEFORE vector search = much better results\n  Multi-tenant: separate collections per customer\nHybrid Storage:\n  PostgreSQL: structured data (accounts, orders)\n  Vector DB: embeddings for RAG (Qdrant, pgvector)\n  S3/MongoDB: raw documents (PDFs, images)\n  pgvector: one DB for both. Good under 10M vectors.\nData Freshness:\n  Incremental: re-embed only changed docs (track last_modified)\n  Full re-index: when changing embedding model or chunking\n  Version tagging: build new index, swap atomically. Zero downtime.\nMonitor: vector count, query latency, stale document count.",interview:"SAY THIS: 'I design hybrid storage: PostgreSQL for structured data, dedicated vector DB for semantic search, and document store for raw files. Metadata filtering is critical -- I pre-filter on source, date, and type before vector search. Data freshness via incremental re-indexing with version-tagged atomic swaps.'\n\nQ: How do you handle re-indexing when changing embedding models?\nA: 'Full re-index into a new collection. Build alongside the old one. When complete, atomic swap: update the alias to point to the new collection. Zero downtime. The old collection stays as rollback for 48 hours.'\n\nQ: pgvector vs dedicated vector DB?\nA: 'pgvector under 10M vectors: simpler architecture, one database. Above 10M: dedicated Qdrant or Pinecone for better query performance and specialized features like multi-vector search.'",code:"# Hybrid storage architecture\nfrom dataclasses import dataclass, field\nfrom datetime import datetime, timedelta\nfrom typing import Optional\n\n@dataclass\nclass VectorDBSchema:\n    \"\"\"Schema for a vector DB collection\"\"\"\n    collection: str\n    vector_dim: int = 1536\n    metadata_fields: list = field(default_factory=lambda: [\n        \"source\", \"doc_type\", \"department\",\n        \"date_updated\", \"chunk_index\", \"content_hash\"\n    ])\n\n@dataclass\nclass HybridArchitecture:\n    \"\"\"Three-store architecture\"\"\"\n    relational: str = \"PostgreSQL\"    # Accounts, orders, billing\n    vector: str = \"Qdrant\"            # Embeddings for RAG\n    document: str = \"S3\"              # Raw PDFs, images\n    \n    def query_flow(self, user_id, question):\n        print(f\"  1. [{self.relational}] Get user context: account, plan, history\")\n        print(f\"  2. [{self.vector}] Search relevant docs (filtered by department)\")\n        print(f\"  3. [{self.document}] Fetch original PDF if needed\")\n        print(f\"  4. [LLM] Generate answer from all sources\")\n\n# Freshness checker\ndef check_freshness(docs, max_age_days=30):\n    stale = []\n    cutoff = datetime.now() - timedelta(days=max_age_days)\n    for doc in docs:\n        if doc[\"updated\"] < cutoff:\n            stale.append(doc)\n    return stale\n\n# Demo\nprint(\"VECTOR DB SCHEMA:\")\nschema = VectorDBSchema(collection=\"support_docs\")\nprint(f\"  Collection: {schema.collection}\")\nprint(f\"  Dimensions: {schema.vector_dim}\")\nprint(f\"  Metadata: {schema.metadata_fields}\")\n\nprint(\"\\nHYBRID QUERY FLOW:\")\narch = HybridArchitecture()\narch.query_flow(\"user-123\", \"What is my refund status?\")\n\nprint(\"\\nFRESHNESS CHECK:\")\ndocs = [{\"name\":\"policy.pdf\",\"updated\":datetime(2024,1,1)},\n        {\"name\":\"pricing.pdf\",\"updated\":datetime.now()}]\nstale = check_freshness(docs)\nfor d in stale: print(f\"  STALE: {d['name']} (updated {d['updated'].date()})\")\nprint(f\"  {len(stale)}/{len(docs)} documents need re-indexing\")",output:"VECTOR DB SCHEMA:\n  Collection: support_docs\n  Dimensions: 1536\n  Metadata: ['source', 'doc_type', 'department', 'date_updated', 'chunk_index', 'content_hash']\n\nHYBRID QUERY FLOW:\n  1. [PostgreSQL] Get user context: account, plan, history\n  2. [Qdrant] Search relevant docs (filtered by department)\n  3. [S3] Fetch original PDF if needed\n  4. [LLM] Generate answer from all sources\n\nFRESHNESS CHECK:\n  STALE: policy.pdf (updated 2024-01-01)\n  1/2 documents need re-indexing\n\n# Three stores, each doing what it does best.\n# Metadata filtering + freshness checks = accurate, current answers."}
]}
];
const L=[{n:"litellm",u:"Gateway proxy, 100+ providers",i:"pip install litellm"},{n:"gptcache",u:"Semantic caching",i:"pip install gptcache"},{n:"tenacity",u:"Retry + circuit breaker",i:"pip install tenacity"},{n:"qdrant-client",u:"Vector DB (production)",i:"pip install qdrant-client"},{n:"pgvector",u:"PostgreSQL vector extension",i:"CREATE EXTENSION vector"},{n:"unstructured",u:"Document extraction",i:"pip install unstructured"}];
const AN=D.flatMap(s=>s.topics.map(t=>({name:t.name,section:s.title,pocket:t.pocket,interview:t.interview})));

export default function App(){
  const[sec,setSec]=useState(D[0].id);
  const[top,setTop]=useState(0);
  const[openPanels,setOpenPanels]=useState({teach:true,code:true});
  const[mode,setMode]=useState("learn");
  const[search,setSearch]=useState("");
  
  const S=D.find(s=>s.id===sec);
  const T=S?.topics[top];
  
  const toggle=k=>setOpenPanels(p=>({...p,[k]:!p[k]}));

  // Filter for pocket/interview modes
  const filtered=search?AN.filter(a=>a.name.toLowerCase().includes(search.toLowerCase())||a.pocket.toLowerCase().includes(search.toLowerCase())):AN;

  const Panel=({id,label,icon,color,gradient,children})=>{
    const isOpen=openPanels[id]!==undefined?openPanels[id]:(id==="teach"||id==="code");
    return(
      <div style={{marginBottom:10,borderRadius:12,overflow:"hidden",border:"1px solid rgba(255,255,255,0.06)",background:"rgba(255,255,255,0.02)"}}>
        <button onClick={()=>toggle(id)} style={{width:"100%",padding:"12px 16px",border:"none",background:isOpen?`linear-gradient(135deg,${gradient})`:("rgba(255,255,255,0.03)"),color:isOpen?"#fff":color,fontSize:12,fontWeight:700,cursor:"pointer",textAlign:"left",fontFamily:"inherit",display:"flex",justifyContent:"space-between",alignItems:"center",letterSpacing:0.8,textTransform:"uppercase",transition:"all 0.2s"}}>
          <span>{icon} {label}</span>
          <span style={{fontSize:16,transition:"transform 0.3s",transform:isOpen?"rotate(180deg)":"rotate(0)"}}>{isOpen?"▾":"▸"}</span>
        </button>
        {isOpen&&<div style={{padding:"14px 16px",borderTop:"1px solid rgba(255,255,255,0.04)"}}>{children}</div>}
      </div>
    );
  };

  const Pre=({color,children})=>(
    <pre style={{fontSize:12,lineHeight:1.75,color:color||"#c9d1d9",whiteSpace:"pre-wrap",margin:0,fontFamily:"'SF Mono','Fira Code','Courier New',monospace",wordBreak:"break-word",letterSpacing:0.2}}>{children}</pre>
  );

  const NoteCard=({item,i,color,field})=>(
    <div style={{background:"rgba(255,255,255,0.03)",border:"1px solid rgba(255,255,255,0.06)",borderRadius:12,marginBottom:12,overflow:"hidden",transition:"transform 0.2s"}}>
      <div style={{padding:"12px 16px",background:"rgba(255,255,255,0.04)",borderBottom:"1px solid rgba(255,255,255,0.05)",display:"flex",justifyContent:"space-between",alignItems:"center"}}>
        <span style={{fontSize:13,fontWeight:700,color}}>{i+1}. {item.name}</span>
        <span style={{fontSize:10,color:"#6e7681",background:"rgba(255,255,255,0.05)",padding:"3px 10px",borderRadius:20}}>{item.section}</span>
      </div>
      <div style={{padding:"14px 16px"}}>
        <Pre color={field==="pocket"?"#b5e8c3":"#d2a8ff"}>{item[field]}</Pre>
      </div>
    </div>
  );

  return(
    <div style={{fontFamily:"'Inter','SF Pro Display',-apple-system,system-ui,sans-serif",background:"#08080c",color:"#e0e0e0",minHeight:"100vh"}}>
      <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&family=Fira+Code:wght@400;500&display=swap" rel="stylesheet"/>
      
      {/* HEADER */}
      <div style={{background:"linear-gradient(135deg,#0f0a2e 0%,#0d1117 40%,#0a1a1a 100%)",padding:"24px 20px 20px",borderBottom:"1px solid rgba(255,255,255,0.06)"}}>
        <div style={{fontSize:11,fontWeight:600,color:"#e3b341",textTransform:"uppercase",letterSpacing:2,marginBottom:6}}>Senior Architect Masterclass</div>
        <div style={{fontSize:26,fontWeight:900,background:"linear-gradient(135deg,#e3b341,#a78bfa,#f472b6)",WebkitBackgroundClip:"text",WebkitTextFillColor:"transparent",lineHeight:1.2}}>Architecture Patterns</div>
        <div style={{fontSize:12,color:"#6e7681",marginTop:6}}>{D.reduce((a,s)=>a+s.topics.length,0)} Topics across {D.length} Sections — Learn, Revise, Interview</div>
      </div>

      {/* MODE TABS */}
      <div style={{display:"flex",gap:6,padding:"10px 16px",background:"rgba(255,255,255,0.01)",borderBottom:"1px solid rgba(255,255,255,0.04)"}}>
        {[["learn","Learn + Code","#e3b341"],["pocket","Pocket Notes","#3fb950"],["interview","Interview Prep","#d2a8ff"]].map(([m,label,c])=>(
          <button key={m} onClick={()=>setMode(m)} style={{flex:1,padding:"10px",borderRadius:10,border:mode===m?`2px solid ${c}`:"2px solid transparent",background:mode===m?`${c}10`:"rgba(255,255,255,0.03)",color:mode===m?c:"#6e7681",fontSize:11,fontWeight:700,cursor:"pointer",fontFamily:"inherit",transition:"all 0.2s",letterSpacing:0.5}}>{label}</button>
        ))}
      </div>

      {/* POCKET / INTERVIEW MODES */}
      {mode!=="learn"&&(
        <div style={{padding:"16px"}}>
          <input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search topics..." style={{width:"100%",padding:"10px 14px",borderRadius:10,border:"1px solid rgba(255,255,255,0.08)",background:"rgba(255,255,255,0.03)",color:"#e0e0e0",fontSize:13,fontFamily:"inherit",marginBottom:14,outline:"none"}}/>
          <div style={{fontSize:12,color:"#6e7681",marginBottom:12}}>{filtered.length} topics</div>
          {filtered.map((item,i)=>(
            <NoteCard key={i} item={item} i={i} color={mode==="pocket"?"#3fb950":"#d2a8ff"} field={mode==="pocket"?"pocket":"interview"}/>
          ))}
        </div>
      )}

      {/* LEARN MODE */}
      {mode==="learn"&&(<>
        {/* SECTION NAV */}
        <div style={{display:"flex",gap:4,padding:"8px 12px",overflowX:"auto",borderBottom:"1px solid rgba(255,255,255,0.04)",background:"rgba(255,255,255,0.01)"}}>
          {D.map(s=>(
            <button key={s.id} onClick={()=>{setSec(s.id);setTop(0);setOpenPanels({teach:true,code:true});}} style={{padding:"8px 14px",borderRadius:20,border:sec===s.id?"1px solid #e3b341":"1px solid transparent",background:sec===s.id?"rgba(88,166,255,0.1)":"transparent",color:sec===s.id?"#e3b341":"#6e7681",fontSize:11,fontWeight:600,cursor:"pointer",whiteSpace:"nowrap",fontFamily:"inherit",transition:"all 0.2s"}}>{s.icon} {s.title}</button>
          ))}
        </div>

        {/* SECTION HEADER */}
        <div style={{padding:"16px 20px 8px"}}>
          <div style={{fontSize:20,fontWeight:800,color:"#e6edf3",letterSpacing:-0.3}}>{S?.title}</div>
          <div style={{fontSize:12,color:"#e3b341",marginTop:3,fontWeight:500}}>{S?.sub}</div>
          {S?.intro&&(
            <div style={{fontSize:12,lineHeight:1.7,color:"#8b949e",marginTop:10,padding:"12px 16px",background:"rgba(255,255,255,0.02)",borderRadius:10,borderLeft:"3px solid #e3b341"}}>
              {S.intro}
            </div>
          )}
        </div>

        {/* TOPIC SELECTOR */}
        {S?.topics.length>1&&(
          <div style={{display:"flex",gap:6,padding:"8px 20px 12px",flexWrap:"wrap"}}>
            {S.topics.map((t,i)=>(
              <button key={i} onClick={()=>{setTop(i);setOpenPanels({teach:true,code:true});}} style={{padding:"6px 16px",borderRadius:20,border:"none",background:top===i?"linear-gradient(135deg,#e3b341,#a78bfa)":"rgba(255,255,255,0.04)",color:top===i?"#fff":"#8b949e",fontSize:11,fontWeight:top===i?700:500,cursor:"pointer",fontFamily:"inherit",transition:"all 0.2s"}}>{i+1}. {t.name}</button>
            ))}
          </div>
        )}

        {/* TOPIC CONTENT */}
        {T&&(
          <div style={{padding:"4px 20px 20px"}}>
            <div style={{background:"rgba(255,255,255,0.03)",borderRadius:12,padding:"14px 16px",marginBottom:12,borderLeft:"3px solid #e3b341"}}>
              <div style={{fontSize:16,fontWeight:800,color:"#e6edf3"}}>{T.name}</div>
            </div>

            <Panel id="teach" label="Deep Teach" icon="📖" color="#a78bfa" gradient="#7c3aed,#a78bfa"><Pre>{T.teach}</Pre></Panel>
            {T.tip&&<Panel id="tip" label="Architect Tips + War Stories" icon="🏗" color="#f0883e" gradient="#ea580c,#f0883e"><Pre color="#f0883e">{T.tip}</Pre></Panel>}
            {T.pocket&&<Panel id="pocket" label="Pocket Notes" icon="📋" color="#3fb950" gradient="#16a34a,#3fb950"><Pre color="#3fb950">{T.pocket}</Pre></Panel>}
            {T.interview&&<Panel id="interview" label="Interview Prep" icon="🎯" color="#d2a8ff" gradient="#8b5cf6,#d2a8ff"><Pre color="#d2a8ff">{T.interview}</Pre></Panel>}
            
            <Panel id="code" label="Code" icon="⌨️" color="#3fb950" gradient="#166534,#238636">
              <div style={{background:"#0d1117",borderRadius:8,overflow:"hidden"}}>
                <div style={{padding:"8px 12px",background:"rgba(255,255,255,0.03)",borderBottom:"1px solid rgba(255,255,255,0.05)",display:"flex",alignItems:"center",gap:8}}>
                  <div style={{width:8,height:8,borderRadius:"50%",background:"#f85149"}}/>
                  <div style={{width:8,height:8,borderRadius:"50%",background:"#d29922"}}/>
                  <div style={{width:8,height:8,borderRadius:"50%",background:"#3fb950"}}/>
                  <span style={{fontSize:10,color:"#6e7681",marginLeft:8}}>Python</span>
                </div>
                <pre style={{padding:14,margin:0,overflow:"auto",fontSize:11.5,lineHeight:1.65,color:"#c9d1d9",fontFamily:"'Fira Code','SF Mono','Courier New',monospace",maxHeight:450}}><code>{T.code}</code></pre>
              </div>
            </Panel>

            {T.output&&<Panel id="output" label="Output" icon="📺" color="#d29922" gradient="#92400e,#d29922"><Pre color="#d29922">{T.output}</Pre></Panel>}
          </div>
        )}

        {/* LIBRARIES */}
        <div style={{margin:"0 20px 20px",background:"rgba(255,255,255,0.02)",border:"1px solid rgba(255,255,255,0.06)",borderRadius:12,padding:16}}>
          <div style={{fontSize:11,fontWeight:700,color:"#e3b341",marginBottom:10,textTransform:"uppercase",letterSpacing:1.5}}>Libraries</div>
          {L.map((l,i)=>(
            <div key={i} style={{display:"flex",justifyContent:"space-between",alignItems:"center",padding:"8px 0",borderBottom:i<L.length-1?"1px solid rgba(255,255,255,0.04)":"none",flexWrap:"wrap",gap:6}}>
              <span><span style={{color:"#e3b341",fontWeight:600,fontSize:12}}>{l.n}</span><span style={{color:"#6e7681",fontSize:11,marginLeft:8}}>{l.u}</span></span>
              <code style={{fontSize:10,color:"#7ee787",background:"rgba(63,185,80,0.1)",padding:"3px 10px",borderRadius:6,fontFamily:"'Fira Code',monospace"}}>{l.i}</code>
            </div>
          ))}
        </div>
      </>)}
    </div>
  );
}
