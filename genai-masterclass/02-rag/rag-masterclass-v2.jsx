import { useState } from "react";
const D=[
{id:"pipe",icon:"1",title:"Core RAG Pipeline",sub:"7 steps: document to answer",intro:"Imagine you have a university exam tomorrow, but the exam is open-book. You do not need to memorize everything -- you just need to FIND the right page quickly and read the answer. RAG works the same way. Instead of hoping the LLM memorized the answer during training, you FIND the relevant documents and hand them to the LLM right before it answers.",topics:[
{name:"End-to-End Pipeline",teach:"THE 7 STEPS (think of a librarian helping you):\n\nStep 1 -- INGEST (librarian receives new books):\nLoad your documents into the system. PDFs, web pages, databases, emails -- any source.\n\nStep 2 -- CHUNK (librarian cuts books into flash cards):\nYour documents are too long to fit in the LLM's 'brain' at once. So you split them into small pieces -- like cutting a textbook into individual flash cards. Each card is 500-1000 words.\n\nStep 3 -- EMBED (librarian organizes cards by MEANING, not alphabetically):\nEach flash card gets converted into a list of numbers (called a 'vector'). Cards with similar meanings get similar numbers. 'Machine learning' and 'AI algorithms' get numbers that are close together. 'Chocolate cake recipe' gets numbers that are far away.\n\nStep 4 -- INDEX (librarian files the cards in a special cabinet):\nStore all these numbered cards in a vector database. Think of it as a filing cabinet organized by MEANING instead of by alphabet.\n\nStep 5 -- RETRIEVE (librarian searches for the right cards):\nUser asks a question. The question also gets converted into numbers. Then you search the filing cabinet for cards with the closest numbers. Like asking the librarian: 'find me everything related to refund policies.'\n\nStep 6 -- AUGMENT (librarian hands you the relevant pages):\nTake the found cards and stuff them into the LLM's prompt: 'Here are the relevant documents. Using ONLY these, answer the question.'\n\nStep 7 -- GENERATE (you read the pages and write your answer):\nThe LLM reads the provided context and writes an answer. The answer is grounded in YOUR documents, not hallucinated from training data.",tip:"Imagine you are taking an open-book exam. Your grade depends on TWO things: (1) Did you find the right pages? (2) Did you read them correctly? RAG is the same.\n\n1. START SIMPLE: ChromaDB (free vector database you install with pip) + OpenAI embeddings + recursive chunking. You can have a working RAG system in 1 hour.\n2. 80/20 RULE: 80% of your answer quality comes from chunking and retrieval. Fix how you cut documents and how you search FIRST. Do not buy fancy tools before fixing the basics.\n3. SPEED: The whole pipeline takes about 3 seconds. Embedding 50ms + searching 30ms + reranking 200ms + LLM answer 2 seconds.\n4. COST: About $0.01-0.05 per question. At 100K questions per day, that is $1K-5K per day.\n\nWAR STORY: A team built a fancy RAG system with GraphRAG and reranking. Their answer quality was terrible (60% accurate). Root cause? Their chunks were cutting sentences in half. 'The refund policy is' was in one chunk and '30 days' was in the next. The retriever found 'refund policy' but missed '30 days.' Fixed the chunking, accuracy jumped to 85%. Then they added the fancy stuff and hit 93%. Always fix the basics first.",pocket:"7 Steps: Ingest -> Chunk -> Embed -> Index -> Retrieve -> Augment -> Generate\nAnalogy: open-book exam (find the page, read the answer)\nChunk: 500-1000 words per piece, with 50-100 word overlap\nSpeed: ~3 seconds end-to-end\nCost: $0.01-0.05 per question\nQuality lever #1: how you chunk (fix this FIRST)\nQuality lever #2: how you search (hybrid > just embeddings)\nQuality lever #3: reranking (double-check the top results)\nStart with: ChromaDB + OpenAI embeddings + recursive chunking",interview:"SAY THIS: 'I optimize RAG in priority order: chunking first, then retrieval strategy, then reranking. I have seen teams invest in GraphRAG while their basic chunking was splitting sentences in half. Fix fundamentals before adding complexity.'\n\nQ: RAG vs fine-tuning -- when each?\nA: 'RAG when knowledge changes frequently or you need citations. Fine-tuning when you need to change the model's behavior (tone, format). Best approach: fine-tune for format + RAG for knowledge.'\n\nQ: Your RAG hallucinates 15% of the time. Debug process?\nA: 'Isolate the step: (1) Wrong docs retrieved? Fix chunking/embedding. (2) Right docs but wrong answer? Chunks too big (noise) or too small (missing context). (3) Context is correct but LLM ignores it? Strengthen prompt with ONLY use context + citation requirement.'",code:"from langchain_openai import OpenAIEmbeddings, ChatOpenAI\nfrom langchain_community.vectorstores import Chroma\nfrom langchain.text_splitter import RecursiveCharacterTextSplitter\nfrom langchain_core.prompts import ChatPromptTemplate\nfrom langchain_core.output_parsers import StrOutputParser\n\ndocs = [\"Refund: 30 days. Electronics: 15 days. Must be unused.\",\n        \"Warranty: 1 year for defects. Damage NOT covered.\",\n        \"Pricing: Pro $49/mo. Enterprise $199/mo. Annual: 20% off.\"]\nchunks = RecursiveCharacterTextSplitter(chunk_size=200,chunk_overlap=30).create_documents(docs)\nvs = Chroma.from_documents(chunks, OpenAIEmbeddings(model=\"text-embedding-3-small\"))\nretrieved = vs.as_retriever(search_kwargs={\"k\":3}).invoke(\"Return electronics after 20 days?\")\ncontext = \"\\n\".join([d.page_content for d in retrieved])\n\nanswer = (ChatPromptTemplate.from_template(\n    \"Using ONLY this context, answer. If not found, say 'I do not know.'\\nContext:{c}\\nQ:{q}\")\n    | ChatOpenAI(model=\"gpt-4\") | StrOutputParser()\n).invoke({\"c\":context,\"q\":\"Can I return electronics after 20 days?\"})\nprint(answer)",output:"No, electronics have a 15-day return window, shorter than the 30-day standard.\n\n# The LLM read YOUR documents and found the right answer.\n# Without RAG, it would have guessed (maybe wrong)."}
]},
{id:"chunk",icon:"2",title:"Chunking Strategies",sub:"How you cut = how good your answers",intro:"Imagine you have a textbook and you need to make flash cards. If you cut a page in the middle of a sentence -- 'The refund policy is' on one card and '30 days for unused items' on another -- neither card is useful alone. Good chunking = smart cutting that keeps complete ideas together.",topics:[
{name:"Fixed + Recursive + Semantic + Parent-Child",teach:"4 WAYS TO CUT YOUR DOCUMENTS:\n\n1. FIXED-SIZE (the dumb way):\nImagine cutting a rope into equal 100cm pieces. Fast and easy. But what if the knot is at position 99? You cut through it.\n\nFixed chunking splits every N characters regardless of content. It is fast but often cuts mid-sentence. Use it only for very uniform text like log files.\n\n2. RECURSIVE (the smart default -- use this 90% of the time):\nImagine a smart editor reading your document. First, they try to split at paragraph breaks. If a paragraph is still too big, they split at sentence boundaries. If a sentence is somehow still too long, they split at word boundaries.\n\nIt respects the document's natural structure. Paragraphs stay together. Sentences stay complete. This is what you should use for almost everything.\n\n3. SEMANTIC (the expensive genius):\nImagine a human reader who says 'the topic changed here.' They UNDERSTAND the content and split where the MEANING shifts.\n\nHow it works: embed every sentence, then compare neighboring sentences. Where the similarity DROPS = the topic changed = split here.\n\nHighest quality but expensive (you embed every sentence during chunking). Use for high-value documents like legal contracts or medical papers.\n\n4. PARENT-CHILD (the production standard):\nThis is the one most real systems use. The problem with all the above: small chunks give precise search results, but the LLM needs more context to write a good answer.\n\nSolution: make TWO versions of your chunks.\n  CHILDREN (small, 128 words): used for SEARCHING. Like an index card that says 'refund policy: 15 days for electronics.'\n  PARENTS (big, 512 words): sent to the LLM. Like the full page that the index card points to.\n\nYou search the index cards (precise), then hand the LLM the full page (context-rich). Best of both worlds.\n\nImagine a library card catalog. The small card tells you exactly where to look. The big book gives you the full story.",tip:"1. RECURSIVE is your default. Do not overthink it. Use the others only when recursive is not good enough.\n2. Chunk size sweet spot: 512 words for searching, 1024 words for sending to the LLM.\n3. OVERLAP: Make each chunk share 10-20% of text with the next chunk. This prevents losing context at boundaries. Like flash cards where each card repeats the last sentence of the previous card.\n4. Custom separators for code: split at class definitions and function definitions, not randomly.\n5. LATE CHUNKING (2024, new): Embed the FULL document first, THEN split. Each chunk 'remembers' the whole document. Solves the 'It' problem: if a chunk says 'It was founded in 1998,' the chunk knows 'It' refers to 'Acme Corp' from page 1. Requires special embedding models (Jina 8K+).\n6. COST: Recursive costs $0.001 per document. Semantic costs $0.10 per document (100x more). At 1 million documents: $1K vs $100K.",pocket:"Fixed: cut every N characters. Fast, dumb, splits sentences.\nRecursive: paragraphs -> sentences -> words. THE DEFAULT (90%).\nSemantic: embed sentences, split at topic changes. Expensive but best quality.\nParent-Child: small chunks for search, big chunks for LLM. PRODUCTION STANDARD.\nAnalogy: library index cards (small, precise) pointing to full book pages (big, context-rich)\nChunk size: 512 for search, 1024 for LLM. Overlap: 10-20%.\nLate Chunking: embed full doc first, then split. Solves pronoun problem. New (2024).\nCost: recursive $0.001/doc vs semantic $0.10/doc",interview:"SAY THIS: 'I default to recursive chunking for 90% of cases. In production, I use parent-child: small chunks for precise search, big parent chunks for the LLM context. This solves the fundamental tension between search precision and generation quality.'\n\nQ: How do you determine optimal chunk size?\nA: 'Empirically. I test 256, 512, and 1024 on my eval set, measuring retrieval precision and answer quality. Smaller = more precise search but less context. I usually land on 512 for search with parent chunks of 1024.'\n\nQ: Why not just use big chunks for everything?\nA: 'A 1000-word chunk about 3 topics produces an embedding that is the AVERAGE of all 3 topics. It matches none of them well. A 128-word chunk about 1 topic produces a laser-focused embedding. But 128 words is not enough context for the LLM. Parent-child gives both.'",code:"from langchain.text_splitter import RecursiveCharacterTextSplitter\nfrom langchain_openai import OpenAIEmbeddings\nfrom langchain_community.vectorstores import Chroma\nfrom langchain.storage import InMemoryStore\nfrom langchain.retrievers import ParentDocumentRetriever\n\ndoc = [\"\"\"Acme refund: standard 30 days. Electronics 15 days.\nDigital products non-refundable once activated.\nShipping covered for defects only.\"\"\"]\n\n# PARENT-CHILD: small for search, big for LLM\nretriever = ParentDocumentRetriever(\n    vectorstore=Chroma(collection_name=\"c\",\n        embedding_function=OpenAIEmbeddings(model=\"text-embedding-3-small\")),\n    docstore=InMemoryStore(),\n    child_splitter=RecursiveCharacterTextSplitter(chunk_size=80),\n    parent_splitter=RecursiveCharacterTextSplitter(chunk_size=300))\nretriever.add_documents(doc)\nresults = retriever.invoke(\"Return electronics after 20 days?\")\nprint(f\"Searched small CHILDREN, returned big PARENT ({len(results[0].page_content)} chars)\")\nprint(results[0].page_content[:100]+\"...\")",output:"Searched small CHILDREN, returned big PARENT (198 chars)\nAcme refund: standard 30 days. Electronics 15 days. Digital products non-refundable...\n\n# Child matched on 'Electronics 15 days' (precise)\n# But the LLM gets the FULL policy with ALL context"}
]},
{id:"embed",icon:"3",title:"Embeddings + Vector DBs",sub:"Turn words into numbers, search by meaning",intro:"Imagine you could organize a library not alphabetically, but by MEANING. All books about 'cooking' would be near each other, even if one is titled 'Italian Cuisine' and another is 'Quick Weeknight Meals.' Embeddings do this: they turn text into numbers where similar meanings have similar numbers. Vector databases are the shelves that hold these numbered books.",topics:[
{name:"6 Models + 7 Databases",teach:"EMBEDDINGS -- HOW THEY WORK:\nTake any text: 'Machine learning is cool'\nConvert it to a list of numbers: [0.12, -0.34, 0.56, ...] (1536 numbers)\n\nNow take: 'AI algorithms are awesome'\nConvert it: [0.11, -0.33, 0.55, ...] (very SIMILAR numbers!)\n\nAnd: 'Chocolate cake recipe'\nConvert it: [0.89, 0.42, -0.71, ...] (very DIFFERENT numbers)\n\nWhen you search, you compare numbers. Close numbers = similar meaning. That is how the LLM 'understands' your question matches your documents even when the exact words differ.\n\n6 EMBEDDING MODELS (which one to pick):\n1. OpenAI text-embedding-3-small: Best balance of quality and cost. $0.02 per million words. Start here.\n2. OpenAI text-embedding-3-large: Highest quality API model. 6x more expensive. For when quality is everything.\n3. all-MiniLM-L6-v2: FREE. Runs on your computer. Good enough for prototypes.\n4. BGE-large: FREE. Runs on your computer. Almost as good as OpenAI.\n5. Jina v3: Supports long documents (8K words). Good for late chunking.\n6. Cohere Embed v3: Great for multiple languages.\n\nSHRINK TRICK (Matryoshka):\nOpenAI models let you use fewer numbers per embedding: 1536 -> 256.\nThis saves 6x storage space with only 5% quality loss. Like compressing a photo.\n\n7 VECTOR DATABASES (where to store embeddings):\nPrototype: ChromaDB (install with pip, works in memory, zero setup)\nAlready use Postgres: pgvector extension (no new database to manage)\nProduction: Qdrant (fast, great filters, self-hosted)\nHuge scale (billions): Milvus or Pinecone (managed, no ops)\n\nWARNING -- LOCK-IN:\nIf you change your embedding model later, you must re-embed ALL your documents. Choose carefully upfront.",tip:"1. Start with OpenAI text-embedding-3-small. Best value.\n2. Use the Matryoshka trick: 256 dimensions instead of 1536 = 6x less storage, 95% quality.\n3. ALWAYS test on YOUR data. The leaderboard rankings are for general text. Your medical documents or legal contracts might behave differently.\n4. pgvector works great under 10 million documents. After that, it slows down. Qdrant handles 50 million easily.\n5. Storage math: 10 million documents x 1536 numbers x 4 bytes = 60 GB. With 256 numbers: only 10 GB.\n6. Changing embedding models is the most expensive operation in a RAG system. Like switching from metric to imperial in a factory -- everything needs to be remeasured.",pocket:"Embeddings: turn text into numbers. Similar meaning = similar numbers.\nModels: OpenAI-small (best value) | MiniLM (free) | BGE (free+quality)\nMatryoshka trick: 1536 -> 256 dimensions = 6x savings, 95% quality\nDatabases: ChromaDB (prototype) | pgvector (have Postgres) | Qdrant (production) | Pinecone (managed)\nStorage: 10M docs x 1536d = 60GB | 10M x 256d = 10GB\nLock-in: changing embedding model = re-embed EVERYTHING\npgvector: great under 10M, slows after\nAlways benchmark on YOUR data, not leaderboard\nEmbedding cost: 10M docs = ~$100 one-time",interview:"SAY THIS: 'I use OpenAI text-embedding-3-small with Matryoshka dimension reduction to 256. This gives the best cost-quality ratio -- 6x storage savings with only 5% quality loss. For databases, I match to scale: pgvector under 10M vectors, Qdrant for production, Pinecone for zero-ops.'\n\nQ: How do you handle embedding model migration?\nA: 'It is the most expensive RAG operation. I dual-write new embeddings while keeping old ones running. Shadow mode: query both, compare results. Atomic cutover when confidence is high. Budget: re-embedding 10M docs costs about $100 plus pipeline downtime.'",code:"from openai import OpenAI\nimport numpy as np\nclient = OpenAI()\n\ntexts = [\"Machine learning uses data to find patterns\",\n         \"AI algorithms learn from datasets\",\n         \"Weather forecast predicts rain\"]\nresp = client.embeddings.create(model=\"text-embedding-3-small\",input=texts)\nembs = [r.embedding for r in resp.data]\n\ndef cos(a,b): return np.dot(a,b)/(np.linalg.norm(a)*np.linalg.norm(b))\nprint(f\"ML vs AI:      {cos(embs[0],embs[1]):.3f} (HIGH = similar meaning!)\")\nprint(f\"ML vs Weather: {cos(embs[0],embs[2]):.3f} (LOW = different topics)\")",output:"ML vs AI:      0.912 (HIGH = the model knows these are related!)\nML vs Weather: 0.234 (LOW = the model knows these are unrelated)\n\n# Even though 'machine learning' and 'AI algorithms' use different words,\n# the embeddings are close because the MEANING is similar."}
]},
{id:"ret",icon:"4",title:"Retrieval Strategies",sub:"Dense + Sparse + Hybrid + Reranking",intro:"Imagine you are searching a library for books about 'automobile safety.' A smart librarian (dense search) finds books about 'car crashes' and 'vehicle protection' -- they understand the MEANING. But they might miss a book literally titled 'Automobile Safety Standards' because they were thinking about meaning, not exact words. A keyword search (sparse) would find that exact title but miss 'car crashes.' Using BOTH = best results.",topics:[
{name:"Hybrid Search + Reranking Pipeline",teach:"4 SEARCH METHODS (use them together):\n\n1. DENSE SEARCH (meaning-based):\nConvert question to numbers (embedding). Find documents with the closest numbers.\n'automobile' matches 'car' because they MEAN the same thing.\nBUT: 'error code 404' might NOT match 'HTTP 404' because the embeddings focus on meaning, and error codes are not really about 'meaning.'\n\n2. SPARSE SEARCH / BM25 (keyword-based):\nOld-school text matching. Count how many words overlap.\n'error code 404' PERFECTLY matches '404 error code found.'\nBUT: 'automobile' does NOT match 'car' because they are different words.\n\nBM25 is free (no embeddings needed) and surprisingly good for technical content.\n\n3. HYBRID (both together -- THE PRODUCTION STANDARD):\nRun BOTH searches. Merge results using a formula called RRF (Reciprocal Rank Fusion).\nThink of it as asking two librarians -- one who understands meaning and one who matches exact words -- and combining their recommendations.\n\nHybrid catches what each search alone would miss.\n\n4. RERANKING (double-check the results):\nAfter hybrid search finds 20 candidate documents, a RERANKER re-scores them.\n\nImagine: the two librarians gave you 20 books. Now a senior librarian reads the question AND each book carefully, and picks the best 5.\n\nThe reranker is more accurate than the initial search because it sees the question AND the document TOGETHER (not just comparing numbers). Adds about 200ms but significantly improves quality.\n\nTHE PRODUCTION PIPELINE:\nQuestion -> hybrid search (find top 20) -> reranker (pick best 5) -> send to LLM",tip:"1. ALWAYS use hybrid in production. Pure dense search misses keyword matches. Pure BM25 misses synonyms. Together they cover each other's weaknesses.\n2. BM25 is FREE and does not need embeddings. Never skip it.\n3. Reranking adds 200ms but improves precision by 10-20%. Worth it for most applications.\n4. For technical documentation and code, weight BM25 higher. Function names and error codes need exact matching.\n5. For conversational queries, weight dense search higher. Users speak naturally, not in keywords.\n6. ALWAYS measure: if your retrieval precision at top 5 results is below 80%, fix retrieval before touching the LLM prompt.",pocket:"Dense: embedding similarity. 'automobile' matches 'car'. Misses exact terms.\nSparse/BM25: keyword matching. 'error 404' matches '404 error'. FREE.\nHybrid: dense + sparse + RRF merge. PRODUCTION STANDARD.\nReranking: senior librarian re-scores top 20 -> picks best 5. +10-20% precision. +200ms.\nPipeline: hybrid top-20 -> rerank top-5 -> LLM\nWeight: code/technical = more BM25. Conversational = more dense.\nMeasure: precision@5 > 80% before optimizing LLM prompt",interview:"SAY THIS: 'My standard pipeline is hybrid search with reranking. Dense for semantic understanding, BM25 for keyword precision, merged via RRF. The reranker re-scores the top 20 to surface the best 5 for the LLM.'\n\nQ: Reranking adds 200ms. Worth it?\nA: 'For enterprise search where answer quality matters? Absolutely. For real-time chatbot where speed is everything? Maybe skip and invest in better chunking. I always benchmark with and without reranking on my eval set.'",code:"from langchain_openai import OpenAIEmbeddings\nfrom langchain_community.vectorstores import Chroma\nfrom rank_bm25 import BM25Okapi\nimport numpy as np\n\ndocs = [\"Python error code 404 means page not found\",\n        \"ML algorithms learn from training data\",\n        \"HTTP 404 status indicates missing resource\",\n        \"AI is transforming technology\",\n        \"Debug Python errors by checking stack trace\"]\n\n# Dense search (by meaning)\nvs = Chroma.from_texts(docs, OpenAIEmbeddings(model=\"text-embedding-3-small\"))\ndense = [d.page_content[:50] for d,_ in vs.similarity_search_with_score(\"Python 404 error\",k=3)]\nprint(\"DENSE (meaning):\",dense)\n\n# BM25 search (by keywords)\nbm25 = BM25Okapi([d.lower().split() for d in docs])\nscores = bm25.get_scores(\"python 404 error\".split())\nsparse = [docs[i][:50] for i in np.argsort(scores)[::-1][:3]]\nprint(\"BM25 (keywords):\",sparse)\n\nprint(\"\\nDense MISSED exact '404'. BM25 nailed it.\")\nprint(\"Hybrid = best of both.\")",output:"DENSE (meaning): ['ML algorithms learn...', 'AI is transforming...', 'Python error code 404...']\nBM25 (keywords):  ['Python error code 404...', 'Debug Python errors...', 'HTTP 404 status...']\n\nDense MISSED exact '404' (ranked it 3rd!). BM25 nailed it (ranked 1st).\nHybrid = best of both."}
]},
{id:"adv",icon:"5",title:"Advanced RAG",sub:"Multi-query, CRAG, Agentic, GraphRAG",intro:"Basic RAG is like asking one librarian one question. Advanced RAG is like having a TEAM of librarians: one rephrases your question 3 different ways to find more books, another checks if the books are actually relevant before handing them to you, and a third can walk between shelves to connect information across multiple books.",topics:[
{name:"Multi-Query + CRAG + Agentic + GraphRAG",teach:"6 ADVANCED PATTERNS (each fixes a specific problem):\n\n1. MULTI-QUERY (fix: 'I cannot find enough documents'):\nImagine you search Google for 'how to reset my password.' You find 2 results. Then you search 'forgot password recovery' and find 3 MORE results. Then 'change account credentials' finds 2 more. By asking the same question 3 different ways, you find way more relevant documents.\n\nMulti-query: LLM rephrases your question 3 ways, searches for each, combines all results. +15-25% more documents found.\n\n2. HyDE (fix: 'my short question does not match long documents'):\nImagine searching for a research paper. Your question is 10 words. The papers are 5000 words. Short questions and long documents have different embeddings.\n\nHyDE trick: ask the LLM to write a FAKE answer first. The fake answer is long, like a real document. Search using the fake answer's embedding instead of the short question's. The fake answer is closer in 'number space' to real documents. +10-20% on complex questions.\n\n3. SELF-RAG (fix: 'I waste money searching for simple questions'):\nImagine someone asks your librarian 'what time is it?' The librarian does NOT need to search any books. They just answer.\n\nSelf-RAG: the LLM first decides IF it even needs to search documents. Simple questions (hi, what is 2+2) skip retrieval entirely. Saves 30-50% of retrieval costs.\n\n4. CRAG / Corrective RAG (fix: 'the retrieved documents are irrelevant'):\nImagine your librarian finds 5 books, but 2 are about a completely different topic. If they hand all 5 to you, those 2 irrelevant books confuse you.\n\nCRAG: the LLM GRADES each retrieved document -- 'is this actually relevant? YES or NO.' Irrelevant ones are thrown away BEFORE the LLM tries to answer. Reduces hallucination by 20-40%.\n\n5. AGENTIC RAG (fix: 'I need different search strategies for different questions'):\nImagine one question needs your internal database, another needs a Google search, and a third needs both. A fixed pipeline cannot adapt.\n\nAgentic RAG: an AI agent DECIDES how to search for each question. It can search multiple databases, do web searches, query SQL databases, and combine results. It adapts its strategy per question.\n\n6. GRAPHRAG (fix: 'I need to connect information across documents'):\nImagine someone asks 'Who does Bob ultimately report to?' The answer requires following a chain: Bob reports to Alice. Alice reports to Carol (CEO). This chain spans 3 different documents. Regular search finds individual documents but cannot follow chains.\n\nGraphRAG: builds a knowledge graph (people, projects, relationships) from your documents. Then traverses the graph to follow connections. 'Bob -> reports_to -> Alice -> reports_to -> Carol (CEO)'",tip:"1. MULTI-QUERY: easiest to implement, biggest immediate impact. Start here.\n2. CRAG: catches irrelevant documents before they confuse the LLM. Great for quality.\n3. SELF-RAG: best for saving money. Simple questions skip retrieval entirely.\n4. GRAPHRAG: when more than 20% of your questions need connections across documents.\n5. Implement them as LangGraph state machines with conditional edges.\n6. Do NOT add all patterns at once. Each adds latency and cost. Add the one that fixes your measured weakness.\n\nDECISION: What is your problem?\n  Not finding enough docs? -> multi-query\n  Finding irrelevant docs? -> CRAG\n  Too expensive? -> Self-RAG\n  Need connections across docs? -> GraphRAG\n  Need different search per question? -> Agentic RAG",pocket:"Multi-Query: rephrase question 3 ways, search each, combine. +15-25% recall.\nHyDE: generate fake answer, search with that. +10-20% complex Qs.\nSelf-RAG: skip retrieval for simple Qs. Save 30-50% cost.\nCRAG: grade retrieved docs, throw away irrelevant. -20-40% hallucination.\nAgentic: AI agent decides HOW to search per question.\nGraphRAG: knowledge graph from docs. Multi-hop connections.\nPriority: multi-query (easy win) > CRAG (quality) > Self-RAG (cost) > GraphRAG (connections)\nDo NOT add all at once. Fix your measured weakness.",interview:"SAY THIS: 'I layer advanced patterns based on the specific failure I am seeing. Low recall? Multi-query. Hallucination? CRAG. Cost too high? Self-RAG gating. Multi-hop questions? GraphRAG. I diagnose first, then add the right fix.'\n\nQ: GraphRAG -- when is it worth the effort?\nA: 'When more than 20% of questions require information from multiple documents connected by relationships. Entity extraction costs about $2K for 100K documents. The precision improvement on relationship queries is typically 20-40%. If your users mostly ask simple factual questions, GraphRAG is overkill.'",code:"from langchain_openai import ChatOpenAI, OpenAIEmbeddings\nfrom langchain_community.vectorstores import Chroma\nfrom langchain.prompts import ChatPromptTemplate\nfrom langchain_core.output_parsers import StrOutputParser\n\nllm = ChatOpenAI(model=\"gpt-4\")\ndocs = [\"Reset password: Settings > Security > Change.\",\n        \"Recovery: SMS or backup codes.\",\n        \"2FA: Settings > Security > Two-Factor.\",\n        \"Pricing: $29/month Basic.\"]\nvs = Chroma.from_texts(docs, OpenAIEmbeddings(model=\"text-embedding-3-small\"))\n\nq = \"Forgot credentials, how to get back in?\"\n\n# MULTI-QUERY: rephrase 3 ways, search each\nvariations = (ChatPromptTemplate.from_template(\n    \"Generate 3 search queries for: {q}\\nOne per line.\") | llm | StrOutputParser()\n).invoke({\"q\":q}).strip().split(\"\\n\")\nall_docs = set()\nfor query in [q]+variations[:3]:\n    for r in vs.similarity_search(query,k=2): all_docs.add(r.page_content[:40])\nprint(f\"Multi-query found {len(all_docs)} docs (vs 2 with single search)\")\n\n# CRAG: grade each doc -- relevant or not?\nfor doc in list(all_docs):\n    grade = (ChatPromptTemplate.from_template(\n        \"Is this relevant to '{q}'?\\nDoc: {d}\\nYES or NO only.\") | llm | StrOutputParser()\n    ).invoke({\"q\":q,\"d\":doc})\n    print(f\"  [{'KEEP' if 'YES' in grade.upper() else 'TOSS'}] {doc}\")",output:"Multi-query found 3 docs (vs 2 with single search)\n  [KEEP] Reset password: Settings > Security...\n  [KEEP] Recovery: SMS or backup codes...\n  [TOSS] Pricing: $29/month Basic...\n\n# Multi-query: found more relevant docs by rephrasing\n# CRAG: threw away the irrelevant pricing doc before it could confuse the LLM"}
]},
{id:"multi",icon:"6",title:"Multimodal RAG",sub:"Images, tables, PDFs",intro:"Real documents are not just text. A financial report has charts. A medical record has X-ray images. A product manual has diagrams. Multimodal RAG handles ALL of these -- not just the words on the page.",topics:[
{name:"Tables + Images + Vision-Augmented",teach:"THE CHALLENGE:\nImagine your RAG system reads a financial report. The text says 'see Figure 3 for revenue breakdown.' But your system cannot see Figure 3. It is a chart. Your system only reads text. So it skips the most important information.\n\n3 WAYS TO FIX THIS:\n\n1. CONVERT EVERYTHING TO TEXT (simplest, works today):\nTables: convert to markdown format. The LLM understands markdown tables very well.\n  | Product | Revenue |\n  |---|---|\n  | Widget A | $2.1M |\n\nImages/Charts: ask GPT-4o to DESCRIBE the image. 'This is a bar chart showing Widget A at $2.1M and Widget B at $1.8M.' Store that description as a text chunk, just like any other text.\n\nThis is like having someone describe a painting to a blind person. Not perfect, but 90% of the value.\n\n2. MULTIMODAL EMBEDDINGS (embed images directly):\nUse models (CLIP, Jina-CLIP) that can turn BOTH text and images into the same type of numbers. An image of a cat and the text 'a cat' get similar numbers. Then you can search images the same way you search text.\n\n3. VISION-AUGMENTED (send images to the LLM):\nSend the actual chart image to GPT-4o or Claude. The LLM 'looks at' the chart and reads the data directly. Most accurate but most expensive.\n\nPRODUCTION APPROACH:\nConvert tables to markdown + describe images with GPT-4o + embed everything as text.\n90% of the value with your existing text-only infrastructure.",tip:"1. Tables -> markdown. LLMs understand markdown tables well. Just format them with | and ---.\n2. Image description costs about $0.01 per image. For 10,000 images = $100. One-time cost.\n3. Scanned PDFs (where text is actually a picture): use OCR (Tesseract or AWS Textract) first.\n4. The Unstructured library handles PDF/DOCX/HTML parsing automatically. pip install unstructured. It is the Swiss Army knife for document processing.\n5. For critical charts, BOTH describe the chart AND send the original image to the LLM. Belt and suspenders.",pocket:"3 Approaches: text extraction | multimodal embeddings | vision-augmented\nTables -> markdown format (LLMs understand this well)\nImages -> GPT-4o describes them (~$0.01/image)\nScanned PDFs -> OCR (Tesseract, AWS Textract)\nMultimodal embeddings: CLIP, Jina-CLIP. Same numbers for text+images.\nProduction: convert everything to text. 90% of the value.\nUnstructured library: Swiss Army knife for document parsing.\npip install unstructured",interview:"SAY THIS: 'For multimodal RAG, I use a pragmatic approach. Tables become markdown. Images get GPT-4o descriptions. Everything becomes text embeddings in the same vector space. This gives 90% of the value with existing text-only infrastructure.'\n\nQ: User asks about a chart in a PDF. How does your system handle it?\nA: 'Pipeline: Unstructured library extracts the chart image. GPT-4o generates a detailed description including all data points. That description is chunked and embedded like any other text. For maximum accuracy, I also send the original chart image alongside the text context to the LLM.'",code:"# Convert tables to markdown for RAG\ntable = [[\"Product\",\"Revenue\"],[\"Widget A\",\"$2.1M\"],[\"Widget B\",\"$1.8M\"]]\nmd = \"| \" + \" | \".join(table[0]) + \" |\\n|---|---|\\n\"\nfor row in table[1:]:\n    md += \"| \" + \" | \".join(row) + \" |\\n\"\nprint(\"TABLE AS MARKDOWN (LLM reads this easily):\")\nprint(md)\n\n# Describe images with GPT-4o vision\nfrom openai import OpenAI\nclient = OpenAI()\ndesc = client.chat.completions.create(\n    model=\"gpt-4o\",\n    messages=[{\"role\":\"user\",\"content\":[\n        {\"type\":\"text\",\"text\":\"Describe this chart for a search system.\"},\n        {\"type\":\"text\",\"text\":\"[Simulated: bar chart Widget A $2.1M, Widget B $1.8M]\"}]}]\n).choices[0].message.content\nprint(f\"IMAGE -> TEXT: {desc[:100]}...\")\nprint(\"\\nBoth become regular text chunks in the same vector store.\")",output:"TABLE AS MARKDOWN (LLM reads this easily):\n| Product | Revenue |\n|---|---|\n| Widget A | $2.1M |\n| Widget B | $1.8M |\n\nIMAGE -> TEXT: Bar chart comparing Q3 revenue. Widget A leads at $2.1M...\n\nBoth become regular text chunks in the same vector store."}
]},
{id:"eval",icon:"7",title:"RAG Evaluation (RAGAS)",sub:"4 metrics: is your RAG actually working?",intro:"Imagine you built a study assistant for students. How do you know if it WORKS? You need to check 4 things: Did it find the right textbook pages? Did it find ALL the relevant pages? Did it answer using ONLY those pages (not making stuff up)? Did it actually answer the question? RAGAS checks all 4.",topics:[
{name:"Precision + Recall + Faithfulness + Relevance",teach:"4 THINGS TO MEASURE:\n\n1. CONTEXT PRECISION (> 80%): 'Did the librarian find the RIGHT books?'\nImagine the librarian brings you 3 books for your refund question. One is about refunds. One is about weather. One is about cooking. Only 1 out of 3 is relevant = 33% precision. That is terrible.\nFIX: Better chunking. Better search. Add reranking.\n\n2. CONTEXT RECALL (> 75%): 'Did the librarian find ALL the relevant books?'\nThere are 3 books about refunds in the library. The librarian only found 1 = 33% recall. They missed 2 important books.\nFIX: Add hybrid search (BM25 catches what embeddings miss). Multi-query retrieval.\n\n3. FAITHFULNESS (> 90% -- THE MOST CRITICAL): 'Did the student answer using ONLY what the books say?'\nThe book says '30-day refund.' The student writes '60-day refund.' They made it up. That is unfaithful = HALLUCINATION.\nThis is the #1 metric. If below 90%, your system is making things up and users will get wrong answers.\nFIX: Stronger prompt ('ONLY use the provided context'). Citation requirement. CRAG to filter bad docs.\n\n4. ANSWER RELEVANCE (> 85%): 'Did the student actually answer the question?'\nQuestion: 'What is the refund policy?' Answer: 'We have great products!' That does not answer the question.\nFIX: Better generation prompt. Better model.\n\nDIAGNOSTIC MAP (memorize this):\n  Low precision -> your SEARCH is sloppy. Fix chunking or add reranking.\n  Low recall -> your SEARCH is missing docs. Add hybrid search or multi-query.\n  Low faithfulness -> your LLM is MAKING STUFF UP. Fix the prompt, add citations.\n  Low relevance -> your LLM is OFF TOPIC. Fix the generation prompt or use a better model.",tip:"1. FAITHFULNESS is the #1 metric. If below 90%, stop everything and fix it. Your system is lying to users.\n2. Build an eval set of 200+ real questions with known correct answers. Include questions that CANNOT be answered from your docs (system should say 'I do not know').\n3. Run RAGAS in your CI/CD pipeline. If someone changes a prompt and faithfulness drops, the deploy is BLOCKED.\n4. For ongoing monitoring: sample 1% of production traffic, score it automatically, track weekly trends.\n5. Refresh your eval set every 3 months with new real user questions.\n\nWAR STORY: A RAG system had 92% answer relevance (answers sounded good) but only 65% faithfulness. The answers SOUNDED right but contained made-up facts. The LLM was ignoring the context and using its training data. Fix: added 'ONLY use the provided context. If the answer is not in the context, say I do not know.' plus a citation requirement. Faithfulness jumped to 94%.",pocket:"4 RAGAS Metrics:\n  Precision > 80%: found the RIGHT docs? (fix: chunking/reranking)\n  Recall > 75%: found ALL relevant docs? (fix: hybrid/multi-query)\n  Faithfulness > 90%: answer from docs ONLY? (fix: prompt/citations) -- CRITICAL\n  Relevance > 85%: answer addresses question? (fix: prompt/model)\nAnalogy: librarian finds books (precision+recall), student answers from books only (faithfulness), student answers the actual question (relevance)\nEval set: 200+ samples + unanswerable questions\nCI/CD gate: faithfulness < 90% = block deploy\nOnline: sample 1% production, track weekly\npip install ragas deepeval",interview:"SAY THIS: 'Faithfulness is my gating metric. Below 90% means the system is hallucinating -- making things up. I maintain 200+ eval samples, run as a CI/CD gate on every prompt change, and sample 1% of production traffic weekly.'\n\nQ: Faithfulness is 92% but users still complain about wrong answers.\nA: '8% error rate means 1 in 12 answers is wrong. Users notice. Three fixes: add CRAG to grade retrieved docs before generation, require citations so users can verify, and check if errors cluster on specific topics -- those topics need better documentation or specialized retrieval.'\n\nQ: How often should you run RAGAS?\nA: 'Three levels: CI/CD on every prompt change, weekly batch on full eval set, daily 1% production sampling with automated alerts if any metric drops more than 3%.'",code:"from ragas import evaluate\nfrom ragas.metrics import context_precision, context_recall, faithfulness, answer_relevancy\nfrom datasets import Dataset\n\neval_data = {\n    \"question\": [\"Refund policy?\", \"Pro plan cost?\", \"Return electronics after 20 days?\"],\n    \"answer\": [\"Full refund 30 days.\", \"$49/mo, 20% annual discount.\", \"No, 15-day window.\"],\n    \"contexts\": [[\"Refund: 30 days unused.\"],[\"Pro $49/mo. Annual 20% off.\"],[\"Electronics: 15 days.\"]],\n    \"ground_truth\": [\"30 days unused.\", \"$49/mo, 20% annual.\", \"No, 15 days.\"]\n}\n\nresults = evaluate(\n    Dataset.from_dict(eval_data),\n    metrics=[context_precision, context_recall, faithfulness, answer_relevancy])\n\nfor metric, score in results.items():\n    if isinstance(score, float):\n        s = 'PASS' if score > 0.8 else 'WARN' if score > 0.6 else 'FAIL'\n        print(f\"  [{s}] {metric}: {score:.2f}\")",output:"  [PASS] context_precision: 0.94\n  [PASS] context_recall: 0.89\n  [PASS] faithfulness: 0.97\n  [PASS] answer_relevancy: 0.92\n\n# All above 80% = healthy RAG system.\n# Faithfulness 0.97 = minimal hallucination. The system answers from docs, not imagination."}
]}
];
const L=[{n:"langchain",u:"RAG chains",i:"pip install langchain langchain-openai"},{n:"llama-index",u:"RAG indexes",i:"pip install llama-index"},{n:"chromadb",u:"Vector DB (prototype)",i:"pip install chromadb"},{n:"qdrant-client",u:"Vector DB (production)",i:"pip install qdrant-client"},{n:"ragas",u:"RAG evaluation",i:"pip install ragas"},{n:"unstructured",u:"PDF/HTML parsing",i:"pip install unstructured"},{n:"rank_bm25",u:"BM25 keyword search",i:"pip install rank_bm25"}];
const AN=D.flatMap(s=>s.topics.map(t=>({name:t.name,section:s.title,pocket:t.pocket,interview:t.interview})));

export default function App(){
  const[sec,setSec]=useState(D[0].id);
  const[top,setTop]=useState(0);
  const[openPanels,setOpenPanels]=useState({teach:true,code:true});
  const[mode,setMode]=useState("learn");
  const[search,setSearch]=useState("");
  
  const S=D.find(s=>s.id===sec);
  const T=S?.topics[top];
  
  const toggle=k=>setOpenPanels(p=>({...p,[k]:!p[k]}));

  // Filter for pocket/interview modes
  const filtered=search?AN.filter(a=>a.name.toLowerCase().includes(search.toLowerCase())||a.pocket.toLowerCase().includes(search.toLowerCase())):AN;

  const Panel=({id,label,icon,color,gradient,children})=>{
    const isOpen=openPanels[id]!==undefined?openPanels[id]:(id==="teach"||id==="code");
    return(
      <div style={{marginBottom:10,borderRadius:12,overflow:"hidden",border:"1px solid rgba(255,255,255,0.06)",background:"rgba(255,255,255,0.02)"}}>
        <button onClick={()=>toggle(id)} style={{width:"100%",padding:"12px 16px",border:"none",background:isOpen?`linear-gradient(135deg,${gradient})`:("rgba(255,255,255,0.03)"),color:isOpen?"#fff":color,fontSize:12,fontWeight:700,cursor:"pointer",textAlign:"left",fontFamily:"inherit",display:"flex",justifyContent:"space-between",alignItems:"center",letterSpacing:0.8,textTransform:"uppercase",transition:"all 0.2s"}}>
          <span>{icon} {label}</span>
          <span style={{fontSize:16,transition:"transform 0.3s",transform:isOpen?"rotate(180deg)":"rotate(0)"}}>{isOpen?"▾":"▸"}</span>
        </button>
        {isOpen&&<div style={{padding:"14px 16px",borderTop:"1px solid rgba(255,255,255,0.04)"}}>{children}</div>}
      </div>
    );
  };

  const Pre=({color,children})=>(
    <pre style={{fontSize:12,lineHeight:1.75,color:color||"#c9d1d9",whiteSpace:"pre-wrap",margin:0,fontFamily:"'SF Mono','Fira Code','Courier New',monospace",wordBreak:"break-word",letterSpacing:0.2}}>{children}</pre>
  );

  const NoteCard=({item,i,color,field})=>(
    <div style={{background:"rgba(255,255,255,0.03)",border:"1px solid rgba(255,255,255,0.06)",borderRadius:12,marginBottom:12,overflow:"hidden",transition:"transform 0.2s"}}>
      <div style={{padding:"12px 16px",background:"rgba(255,255,255,0.04)",borderBottom:"1px solid rgba(255,255,255,0.05)",display:"flex",justifyContent:"space-between",alignItems:"center"}}>
        <span style={{fontSize:13,fontWeight:700,color}}>{i+1}. {item.name}</span>
        <span style={{fontSize:10,color:"#6e7681",background:"rgba(255,255,255,0.05)",padding:"3px 10px",borderRadius:20}}>{item.section}</span>
      </div>
      <div style={{padding:"14px 16px"}}>
        <Pre color={field==="pocket"?"#b5e8c3":"#d2a8ff"}>{item[field]}</Pre>
      </div>
    </div>
  );

  return(
    <div style={{fontFamily:"'Inter','SF Pro Display',-apple-system,system-ui,sans-serif",background:"#08080c",color:"#e0e0e0",minHeight:"100vh"}}>
      <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&family=Fira+Code:wght@400;500&display=swap" rel="stylesheet"/>
      
      {/* HEADER */}
      <div style={{background:"linear-gradient(135deg,#0f0a2e 0%,#0d1117 40%,#0a1a1a 100%)",padding:"24px 20px 20px",borderBottom:"1px solid rgba(255,255,255,0.06)"}}>
        <div style={{fontSize:11,fontWeight:600,color:"#3fb950",textTransform:"uppercase",letterSpacing:2,marginBottom:6}}>Senior Architect Masterclass</div>
        <div style={{fontSize:26,fontWeight:900,background:"linear-gradient(135deg,#3fb950,#a78bfa,#f472b6)",WebkitBackgroundClip:"text",WebkitTextFillColor:"transparent",lineHeight:1.2}}>RAG</div>
        <div style={{fontSize:12,color:"#6e7681",marginTop:6}}>{D.reduce((a,s)=>a+s.topics.length,0)} Topics across {D.length} Sections — Learn, Revise, Interview</div>
      </div>

      {/* MODE TABS */}
      <div style={{display:"flex",gap:6,padding:"10px 16px",background:"rgba(255,255,255,0.01)",borderBottom:"1px solid rgba(255,255,255,0.04)"}}>
        {[["learn","Learn + Code","#3fb950"],["pocket","Pocket Notes","#3fb950"],["interview","Interview Prep","#d2a8ff"]].map(([m,label,c])=>(
          <button key={m} onClick={()=>setMode(m)} style={{flex:1,padding:"10px",borderRadius:10,border:mode===m?`2px solid ${c}`:"2px solid transparent",background:mode===m?`${c}10`:"rgba(255,255,255,0.03)",color:mode===m?c:"#6e7681",fontSize:11,fontWeight:700,cursor:"pointer",fontFamily:"inherit",transition:"all 0.2s",letterSpacing:0.5}}>{label}</button>
        ))}
      </div>

      {/* POCKET / INTERVIEW MODES */}
      {mode!=="learn"&&(
        <div style={{padding:"16px"}}>
          <input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search topics..." style={{width:"100%",padding:"10px 14px",borderRadius:10,border:"1px solid rgba(255,255,255,0.08)",background:"rgba(255,255,255,0.03)",color:"#e0e0e0",fontSize:13,fontFamily:"inherit",marginBottom:14,outline:"none"}}/>
          <div style={{fontSize:12,color:"#6e7681",marginBottom:12}}>{filtered.length} topics</div>
          {filtered.map((item,i)=>(
            <NoteCard key={i} item={item} i={i} color={mode==="pocket"?"#3fb950":"#d2a8ff"} field={mode==="pocket"?"pocket":"interview"}/>
          ))}
        </div>
      )}

      {/* LEARN MODE */}
      {mode==="learn"&&(<>
        {/* SECTION NAV */}
        <div style={{display:"flex",gap:4,padding:"8px 12px",overflowX:"auto",borderBottom:"1px solid rgba(255,255,255,0.04)",background:"rgba(255,255,255,0.01)"}}>
          {D.map(s=>(
            <button key={s.id} onClick={()=>{setSec(s.id);setTop(0);setOpenPanels({teach:true,code:true});}} style={{padding:"8px 14px",borderRadius:20,border:sec===s.id?"1px solid #3fb950":"1px solid transparent",background:sec===s.id?"rgba(88,166,255,0.1)":"transparent",color:sec===s.id?"#3fb950":"#6e7681",fontSize:11,fontWeight:600,cursor:"pointer",whiteSpace:"nowrap",fontFamily:"inherit",transition:"all 0.2s"}}>{s.icon} {s.title}</button>
          ))}
        </div>

        {/* SECTION HEADER */}
        <div style={{padding:"16px 20px 8px"}}>
          <div style={{fontSize:20,fontWeight:800,color:"#e6edf3",letterSpacing:-0.3}}>{S?.title}</div>
          <div style={{fontSize:12,color:"#3fb950",marginTop:3,fontWeight:500}}>{S?.sub}</div>
          {S?.intro&&(
            <div style={{fontSize:12,lineHeight:1.7,color:"#8b949e",marginTop:10,padding:"12px 16px",background:"rgba(255,255,255,0.02)",borderRadius:10,borderLeft:"3px solid #3fb950"}}>
              {S.intro}
            </div>
          )}
        </div>

        {/* TOPIC SELECTOR */}
        {S?.topics.length>1&&(
          <div style={{display:"flex",gap:6,padding:"8px 20px 12px",flexWrap:"wrap"}}>
            {S.topics.map((t,i)=>(
              <button key={i} onClick={()=>{setTop(i);setOpenPanels({teach:true,code:true});}} style={{padding:"6px 16px",borderRadius:20,border:"none",background:top===i?"linear-gradient(135deg,#3fb950,#a78bfa)":"rgba(255,255,255,0.04)",color:top===i?"#fff":"#8b949e",fontSize:11,fontWeight:top===i?700:500,cursor:"pointer",fontFamily:"inherit",transition:"all 0.2s"}}>{i+1}. {t.name}</button>
            ))}
          </div>
        )}

        {/* TOPIC CONTENT */}
        {T&&(
          <div style={{padding:"4px 20px 20px"}}>
            <div style={{background:"rgba(255,255,255,0.03)",borderRadius:12,padding:"14px 16px",marginBottom:12,borderLeft:"3px solid #3fb950"}}>
              <div style={{fontSize:16,fontWeight:800,color:"#e6edf3"}}>{T.name}</div>
            </div>

            <Panel id="teach" label="Deep Teach" icon="📖" color="#a78bfa" gradient="#7c3aed,#a78bfa"><Pre>{T.teach}</Pre></Panel>
            {T.tip&&<Panel id="tip" label="Architect Tips + War Stories" icon="🏗" color="#f0883e" gradient="#ea580c,#f0883e"><Pre color="#f0883e">{T.tip}</Pre></Panel>}
            {T.pocket&&<Panel id="pocket" label="Pocket Notes" icon="📋" color="#3fb950" gradient="#16a34a,#3fb950"><Pre color="#3fb950">{T.pocket}</Pre></Panel>}
            {T.interview&&<Panel id="interview" label="Interview Prep" icon="🎯" color="#d2a8ff" gradient="#8b5cf6,#d2a8ff"><Pre color="#d2a8ff">{T.interview}</Pre></Panel>}
            
            <Panel id="code" label="Code" icon="⌨️" color="#3fb950" gradient="#166534,#238636">
              <div style={{background:"#0d1117",borderRadius:8,overflow:"hidden"}}>
                <div style={{padding:"8px 12px",background:"rgba(255,255,255,0.03)",borderBottom:"1px solid rgba(255,255,255,0.05)",display:"flex",alignItems:"center",gap:8}}>
                  <div style={{width:8,height:8,borderRadius:"50%",background:"#f85149"}}/>
                  <div style={{width:8,height:8,borderRadius:"50%",background:"#d29922"}}/>
                  <div style={{width:8,height:8,borderRadius:"50%",background:"#3fb950"}}/>
                  <span style={{fontSize:10,color:"#6e7681",marginLeft:8}}>Python</span>
                </div>
                <pre style={{padding:14,margin:0,overflow:"auto",fontSize:11.5,lineHeight:1.65,color:"#c9d1d9",fontFamily:"'Fira Code','SF Mono','Courier New',monospace",maxHeight:450}}><code>{T.code}</code></pre>
              </div>
            </Panel>

            {T.output&&<Panel id="output" label="Output" icon="📺" color="#d29922" gradient="#92400e,#d29922"><Pre color="#d29922">{T.output}</Pre></Panel>}
          </div>
        )}

        {/* LIBRARIES */}
        <div style={{margin:"0 20px 20px",background:"rgba(255,255,255,0.02)",border:"1px solid rgba(255,255,255,0.06)",borderRadius:12,padding:16}}>
          <div style={{fontSize:11,fontWeight:700,color:"#3fb950",marginBottom:10,textTransform:"uppercase",letterSpacing:1.5}}>Libraries</div>
          {L.map((l,i)=>(
            <div key={i} style={{display:"flex",justifyContent:"space-between",alignItems:"center",padding:"8px 0",borderBottom:i<L.length-1?"1px solid rgba(255,255,255,0.04)":"none",flexWrap:"wrap",gap:6}}>
              <span><span style={{color:"#3fb950",fontWeight:600,fontSize:12}}>{l.n}</span><span style={{color:"#6e7681",fontSize:11,marginLeft:8}}>{l.u}</span></span>
              <code style={{fontSize:10,color:"#7ee787",background:"rgba(63,185,80,0.1)",padding:"3px 10px",borderRadius:6,fontFamily:"'Fira Code',monospace"}}>{l.i}</code>
            </div>
          ))}
        </div>
      </>)}
    </div>
  );
}
