# RAG vs Fine-Tune vs Prompt Engineering TCO

## The Problem
Three approaches to customize LLM behavior. Which is cheapest at what volume? Crossover analysis.

## How to Run
```
pip install -r requirements.txt
python main.py
```
http://localhost:8000/metrics | docker-compose up --build | pytest main.py -v
