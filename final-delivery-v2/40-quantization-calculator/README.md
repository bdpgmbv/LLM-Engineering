# Quantization Calculator

## The Problem
See how quantization changes model size, quality, and GPU requirements. No API needed — pure math.

## How to Run
```
pip install -r requirements.txt
python main.py
```
http://localhost:8000/metrics | docker-compose up --build | pytest main.py -v
